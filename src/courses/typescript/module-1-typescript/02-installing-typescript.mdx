---
title: Installing TypeScript
description: រៀនពីរបៀបដំឡើង TypeScript ចូលទៅក្នុងកុំព្យូទ័ររបស់អ្នក និងរបៀបបម្លែង (Compile) កូដ TypeScript ទៅជាកូដ JavaScript ដើម្បីអោយវាអាចដំណើរការបាន។
objectives:
  - ចេះដំឡើង TypeScript តាមរយៈ npm
  - ចេះប្រើប្រាស់ពាក្យបញ្ជា `tsc` ដើម្បី Compile កូដ
  - យល់ពីដំណើរការបម្លែងកូដពី `.ts` ទៅជា `.js`
module: module-1-typescript
order: 2
quiz:
  - question: "តើ Browser ដូចជា Google Chrome អាចអានកូដ `.ts` (TypeScript) ដោយផ្ទាល់បានដែរឬទេ?"
    options:
      - "\"បាន! Chrome ស្គាល់ TypeScript ជាយូរណាស់មកហើយ\""
      - "\"បាន តែត្រូវភ្ជាប់អុីនធឺណិតសិន\""
      - "\"ទេ! Browser ស្គាល់តែ HTML, CSS, និង JavaScript ប៉ុណ្ណោះ។ កូដ TypeScript ត្រូវតែបម្លែង (Compile) ទៅជា JavaScript ជាមុនសិនទើបអាចដំណើរការបាន។\""
      - "\"ទេ! Browser ស្គាល់តែ React ប៉ុណ្ណោះ\""
    correctAnswer: 3
    explanation: "កូដ TypeScript គ្រាន់តែជាកូដសម្រាប់ជួយសម្រួលដល់ 'អ្នកសរសេរកម្មវិធី' អោយមានភាពងាយស្រួលនិងសុវត្ថិភាពតែប៉ុណ្ណោះ។ នៅពេលដែលកម្មវិធីត្រូវបានដាក់អោយដំណើរការ (Run) កូដ TS ទាំងអស់នឹងត្រូវបានលុបប្រភេទ (Types) ចេញ រួចបម្លែងខ្លួនទៅជាកូដ JS សុទ្ធទើបកុំព្យូទ័រឫ Browser អាចយល់បាន។"
---

## ⚙️ ការដំឡើង TypeScript

ដោយសារ TypeScript មិនមែនជាភាសាដើមរបស់ Browser យើងត្រូវតែដំឡើងកម្មវិធីបកប្រែ (Compiler) របស់វា ដើម្បីអាចបម្លែងកូដ `.ts` ទៅជា `.js`។ យើងអាចដំឡើងវាបានតាមរយៈ `npm` (ត្រូវប្រាកដថាអ្នកបានដំឡើង Node.js រួចរាល់)។

បើក Terminal ហើយវាយពាក្យបញ្ជាខាងក្រោម ដើម្បីដំឡើង TypeScript ជាសកល (Global) នៅក្នុងកុំព្យូទ័ររបស់អ្នក៖

```bash
npm install -g typescript
```

ដើម្បីផ្ទៀងផ្ទាត់ថាអ្នកបានដំឡើងវាដោយជោគជ័យ សូមវាយ៖
```bash
tsc -v
```
*(វានឹងបង្ហាញលេខ Version របស់ TypeScript ឧទាហរណ៍ `Version 5.x.x`)*

---

## 🛠️ ការបម្លែងកូដ (Compile)

ឥឡូវនេះយើងសាកល្បងសរសេរកូដ TypeScript ទីមួយរបស់យើង! បង្កើតឯកសារមួយឈ្មោះថា `hello.ts` រួចសរសេរកូដខាងក្រោម៖

```ts
// ឯកសារ: hello.ts
const greeting: string = "សួស្តី TypeScript!";
console.log(greeting);
```

តើយើងរត់ (Run) កូដនេះយ៉ាងដូចម្តេច?
ដូចដែលបានបញ្ជាក់ពីមុន យើងមិនអាច Run កូដ `.ts` ដោយផ្ទាល់បានទេ។ យើងត្រូវប្រាប់អោយកម្មវិធីបកប្រែ TypeScript (TSC) បម្លែងវាទៅជា `.js` សិន។

បើក Terminal ក្នុងថត (Folder) តែមួយនោះ រួចវាយ៖
```bash
tsc hello.ts
```

ភ្លាមៗនោះ អ្នកនឹងឃើញឯកសារថ្មីមួយទៀតបានលេចឡើងដោយស្វ័យប្រវត្តិឈ្មោះថា **`hello.js`**!
បើអ្នកបើកមើល `hello.js` អ្នកនឹងឃើញកូដបែបនេះ៖

```js
// ឯកសារ: hello.js
var greeting = "សួស្តី TypeScript!";
console.log(greeting);
```

ឃើញទេ? ពាក្យ `: string` ត្រូវបានលុបចេញបាត់ហើយ! នេះមានន័យថាកូដរបស់អ្នកឥឡូវនេះជាកូដ JavaScript សុទ្ធ ដែលអាចដំណើរការបានគ្រប់ទីកន្លែង!

ចុងក្រោយ យើងគ្រាន់តែ Run កូដ JavaScript នោះដោយប្រើ Node.js ធម្មតា៖
```bash
node hello.js
```
*(លទ្ធផលនឹងចេញមក: `សួស្តី TypeScript!`)*

> 💡 **គន្លឹះពិសេស:** អ្នកអាចប្រាប់អោយ TypeScript អង្គុយចាំមើលកូដរបស់អ្នក ហើយបម្លែងដោយស្វ័យប្រវត្តិរាល់ពេលដែលអ្នកចុច Save ដោយបន្ថែម `-w` (Watch mode):
> `tsc hello.ts -w`
