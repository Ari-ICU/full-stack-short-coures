---
title: tsconfig.json
description: ស្វែងយល់ពីឯកសារ tsconfig.json ដែលជាបេះដូងនៃការគ្រប់គ្រងគម្រោងរចនាសម្ព័ន្ធ TypeScript និងការកំណត់ច្បាប់វិន័យផ្សេងៗ (Strictness)។
objectives:
  - ចេះបង្កើតឯកសារ tsconfig.json តាមរយៈពាក្យបញ្ជា `tsc --init`
  - យល់ពីការកំណត់សំខាន់ៗដូចជា `target`, `outDir`, `rootDir`
  - 'យល់ពីអត្ថប្រយោជន៍នៃការបើកមុខងារ `strict: true`'
module: module-1-typescript
order: 3
quiz:
  - question: "តើមុខងារ `outDir` នៅក្នុងឯកសារ `tsconfig.json` មានតួនាទីអ្វី?"
    options:
      - "\"វាជាកន្លែងផ្ទុកកូដ HTML\""
      - "\"វាជាថត (Folder) គោលដៅសម្រាប់ផ្ទុកឯកសារ JavaScript ទាំងអស់ដែលបានបម្លែង (Compile) ចេញពីកូដ TypeScript រួចរាល់\""
      - "\"វាជាពាក្យបញ្ជាសម្រាប់លុបកូដចោល\""
      - "\"វាជាលេខសម្ងាត់សម្រាប់ការពារកូដ\""
    correctAnswer: 2
    explanation: "នៅក្នុងគម្រោងធំៗ យើងមិនចង់អោយឯកសារ `.ts` និង `.js` នៅលាយឡំគ្នាពាសពេញនោះទេ។ យើងកំណត់ `outDir: './dist'` ដើម្បីប្រាប់ TypeScript អោយប្រមូលឯកសារ JavaScript ដែល Compile រួចទាំងអស់ យកទៅទុកក្នុងថត `dist` តែមួយគត់។"
---

## 📄 អ្វីទៅជា tsconfig.json?

នៅក្នុងគម្រោងពិតប្រាកដ អ្នកមិនសរសេរកូដត្រឹមតែមួយឯកសារនោះទេ អ្នកប្រហែលជាមានឯកសារ `.ts` រាប់សិប។ ការវាយបញ្ជា `tsc file1.ts`, `tsc file2.ts` គ្រប់ឯកសារគឺមិនអាចទៅរួចទេ។

ដើម្បីអោយ TypeScript ដឹងពីរបៀបបម្លែងកូដ "ទាំងមូល" ក្នុងគម្រោងរបស់អ្នកដោយគ្រាន់តែវាយពាក្យថា `tsc` មួយម៉ាត់ នោះអ្នកត្រូវមានឯកសារកំណត់រចនាសម្ព័ន្ធមួយឈ្មោះថា **`tsconfig.json`**។

ដើម្បីបង្កើតឯកសារនេះ សូមវាយពាក្យបញ្ជាខាងក្រោមនៅក្នុង Folder គម្រោងរបស់អ្នក៖
```bash
tsc --init
```
វានឹងបង្កើតឯកសារ `tsconfig.json` មួយដែលមានផ្ទុកនូវការកំណត់រាប់រយ! ប៉ុន្តែកុំបារម្ភ យើងនឹងផ្តោតតែលើចំណុចសំខាន់ៗប៉ុណ្ណោះ។

---

## 🔧 ការកំណត់សំខាន់ៗដែលត្រូវដឹង

នៅពេលអ្នកបើកឯកសារ `tsconfig.json` អ្នកនឹងឃើញកូដបែបនេះ (ចំណុចខ្លះត្រូវបានបិទដោយ `//`):

```json
{
  "compilerOptions": {
    /* 1. Language and Environment */
    "target": "es2016", // ប្រាប់អោយ TS បម្លែងកូដទៅជា JS ជំនាន់ចាស់កម្រិតណា (ដើម្បីអោយស្គាល់ Browser ចាស់ៗ)
    
    /* 2. Modules */
    "rootDir": "./src", // ប្រាប់ថា Folder មួយណាដែលមានផ្ទុកកូដ .ts ដើមរបស់អ្នក
    
    /* 3. Emit */
    "outDir": "./dist", // ប្រាប់ថាពេលបម្លែងកូដជា .js រួច ត្រូវយកវាទៅទុកនៅ Folder ណា (ដើម្បីកុំអោយរញ៉េរញ៉ៃ)
    
    /* 4. Type Checking */
    "strict": true // សំខាន់បំផុត! បើកច្បាប់វិន័យតឹងរ៉ឹងអោយ TS ចាប់កំហុសអោយបានច្រើនបំផុត។ គួរតែបើកជានិច្ច!
  }
}
```

បន្ទាប់ពីអ្នកបានកែប្រែ (Uncomment) និងកំណត់ `rootDir` ទៅ `./src` និង `outDir` ទៅ `./dist` រួចរាល់ហើយ របៀបធ្វើការរបស់អ្នកនឹងផ្លាស់ប្តូរទៅជាបែបនេះ៖

1. អ្នកដាក់កូដ `.ts` ទាំងអស់នៅក្នុងថត `src/`។
2. អ្នកបើក Terminal រួចវាយពាក្យថា៖
   ```bash
   tsc -w
   ```
3. នោះ TypeScript នឹងអានការកំណត់ក្នុង `tsconfig.json` រួចចាប់ផ្តើមបម្លែងកូដទាំងអស់ក្នុង `src/` យកទៅដាក់ក្នុងថត `dist/` ដោយស្វ័យប្រវត្តិ! 

> **ចំណាំ:** នៅក្នុងគម្រោងសម័យថ្មីដូចជា **Next.js** ឬ **Vite** ឯកសារ `tsconfig.json` នេះនឹងត្រូវបានបង្កើត និងរៀបចំអោយអ្នកដោយស្វ័យប្រវត្តិ តាំងពីពេលអ្នកបង្កើតគម្រោងដំបូងម្ល៉េះ។ ដូច្នេះអ្នកកម្រនឹងត្រូវកែវាដោយផ្ទាល់ណាស់ តែវាមានសារៈសំខាន់ដែលអ្នកត្រូវយល់ពីរបៀបដែលវាដំណើរការ។
