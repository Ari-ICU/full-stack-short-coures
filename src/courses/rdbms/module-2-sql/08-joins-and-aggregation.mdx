---
title: JOINs & Aggregation
description: រៀនពីការភ្ជាប់តារាងបញ្ចួលគ្នា (JOINs) ការបូកសរុបទិន្នន័យ និងការប្រើប្រាស់ HAVING។
objectives:
  - យល់ពីរបៀបប្រើប្រាស់ JOIN (INNER, LEFT, RIGHT, FULL)
  - ប្រើប្រាស់ Aggregate Functions រួមជាមួយ GROUP BY
  - ស្គាល់ពីភាពខុសគ្នារវាង WHERE និង HAVING
module: module-2-sql
order: 8
quiz:
  - question: "តើភាពខុសគ្នាចម្បងរវាង WHERE និង HAVING គឺជាអ្វី?"
    options:
      - "WHERE ប្រើសម្រាប់លេខ ចំណែក HAVING ប្រើសម្រាប់អក្សរ"
      - "WHERE ច្រោះទិន្នន័យមុនពេលចាប់ក្រុម (GROUP BY) ចំណែក HAVING ច្រោះទិន្នន័យក្រោយពេលចាប់ក្រុម"
      - "HAVING ដំណើរការលឿនជាង WHERE"
      - "គ្មានភាពខុសគ្នាទេ យើងអាចប្រើមួយណាក៏បាន"
    correctAnswer: 2
    explanation: "ច្បាប់លំដាប់នៃ SQL គឺ៖ WHERE ប្រើដើម្បីលុបចោលជួរ (rows) ដែលមិនត្រូវលក្ខខណ្ឌ បន្ទាប់មកទើប GROUP BY ចាប់ផ្តើមធ្វើការបូកសរុប។ បន្ទាប់ពីបូកសរុបរួចរាល់ បើយើងចង់ច្រោះលទ្ធផលនៃការបូកសរុបនោះ គឺយើងត្រូវប្រើ HAVING។"
---

## 🤝 ការភ្ជាប់តារាងបញ្ចូលគ្នា (JOINs)

ដោយសារតែ RDBMS បែងចែកទិន្នន័យទៅជាតារាងផ្សេងៗគ្នា (Normalization) ដូច្នេះនៅពេលយើងចង់ទាញយកទិន្នន័យមកប្រើប្រាស់ យើងត្រូវភ្ជាប់តារាងទាំងនោះបញ្ចូលគ្នាវិញដោយប្រើ **JOIN**។

ប្រភេទ JOIN សំខាន់ៗ៖
1. **`INNER JOIN`:** ទាញយកតែទិន្នន័យណាដែលមានទំនាក់ទំនងគ្នានៅក្នុងតារាងទាំងសងខាងប៉ុណ្ណោះ។
2. **`LEFT JOIN`:** ទាញយកទិន្នន័យទាំងអស់ពីតារាងខាងឆ្វេង (តារាងទីមួយ) ទោះបីជាមិនមានទំនាក់ទំនងក្នុងតារាងខាងស្តាំក៏ដោយ (វាចេញជា `NULL`)។
3. **`RIGHT JOIN`:** ផ្ទុយពី LEFT JOIN។
4. **`FULL OUTER JOIN`:** ទាញយកទិន្នន័យទាំងអស់ពីតារាងទាំងពីរ មិនថាមានការផ្គូផ្គង ឬអត់នោះទេ។

**ឧទាហរណ៍ INNER JOIN:** ស្វែងរកឈ្មោះអ្នកទិញ និងតម្លៃសរុបនៃការបញ្ជាទិញរបស់គាត់។
```sql
SELECT users.name, orders.total_price
FROM users
INNER JOIN orders ON users.id = orders.user_id;
```

---

## 📊 អនុគមន៍បូកសរុប (Aggregate Functions)

នៅពេលដែលយើងចង់ធ្វើការគណនាទិន្នន័យជាក្រុម (ឧទាហរណ៍៖ រាប់ចំនួន, បូកសរុបតម្លៃ) យើងប្រើប្រាស់ Aggregate Functions រួមជាមួយ `GROUP BY`។

- `COUNT(*)`: រាប់ចំនួនជួរ (Rows)
- `SUM(column)`: បូកតម្លៃបញ្ចួលគ្នា
- `AVG(column)`: រកមធ្យមភាគ
- `MAX(column)` / `MIN(column)`: រកតម្លៃធំបំផុត / តូចបំផុត

**ឧទាហរណ៍៖** ចង់ដឹងថាតើអ្នកប្រើប្រាស់ម្នាក់ៗ បានចំណាយប្រាក់សរុបប៉ុន្មាន?
```sql
SELECT users.id, users.name, SUM(orders.total_price) AS grand_total
FROM users
LEFT JOIN orders ON users.id = orders.user_id
GROUP BY users.id, users.name;
```

---

## ⚖️ WHERE ទល់នឹង HAVING

នេះគឺជាសំណួរដ៏ពេញនិយមបំផុតនៅពេលសម្ភាសន៍ការងារ (Interview Question)។ 

សន្មតថាអ្នកចង់រកតែអតិថិជនណា ដែលបានបញ្ជាទិញទំនិញ **ធំជាង ៥ ដង** ប៉ុណ្ណោះ។
តើអ្នកអាចប្រើ `WHERE COUNT(*) > 5` បានទេ? **ចម្លើយគឺ ទេ!**

នៅក្នុង SQL លំដាប់នៃការដំណើរការគឺ៖
```text
WHERE    -> ច្រោះទិន្នន័យតាមជួរ (Rows) ជាមុនសិន
  ↓
GROUP BY -> ធ្វើការចាប់ក្រុមទិន្នន័យ
  ↓
HAVING   -> ច្រោះទិន្នន័យដែលបានចាប់ក្រុមរួច (Groups)
```

**ឧទាហរណ៍ត្រឹមត្រូវ៖**
```sql
SELECT user_id, COUNT(*) AS order_count
FROM orders
WHERE status = 'paid'       -- ច្រោះយកតែ orders ណាដែលបានបង់ប្រាក់រួច (Row level)
GROUP BY user_id            -- ចាប់ផ្តើមរាប់ចំនួន orders តាម user ម្នាក់ៗ
HAVING COUNT(*) > 5;        -- ច្រោះយកតែ user ណាដែលមានចំនួន order ធំជាង ៥ (Group level)
```

---

## 📝 ការអនុវត្តជាក់ស្តែង (Mini Case Study)

**លំហាត់:** ចូរអ្នកសរសេរកូដ SQL ដើម្បីស្វែងរកឈ្មោះអ្នកប្រើប្រាស់ និងសរុបចំនួនទឹកប្រាក់ដែលពួកគេបានចំណាយ។ ទាញយកតែអ្នកប្រើប្រាស់ណាដែលចំណាយ **ច្រើនជាង ១០០០ ដុល្លារ** ប៉ុណ្ណោះ។

**ចម្លើយ:**
```sql
SELECT users.name, SUM(orders.total_price) AS total_spent
FROM users
INNER JOIN orders ON users.id = orders.user_id
GROUP BY users.id, users.name
HAVING SUM(orders.total_price) > 1000;
```
