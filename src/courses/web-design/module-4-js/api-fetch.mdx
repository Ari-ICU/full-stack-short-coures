---
title: Asynchronous JavaScript (Fetch API)
description: រៀនពីរបៀបតភ្ជាប់ frontend application របស់អ្នកទៅកាន់ servers ខាងក្រៅ ដោយទាញយកទិន្នន័យពិតប្រាកដតាមរយៈការប្រើប្រាស់ Promises, async/await, និង Fetch API។
objectives:
  - យល់ពីមុខងាររបស់ Fetch API
  - ចេះទាញយកទិន្នន័យ (GET request) ពី API ខាងក្រៅ
  - បង្ហាញទិន្នន័យដែលទទួលបាននៅលើគេហទំព័រ (DOM) ដោយស្វ័យប្រវត្តិ
module: module-4-js
order: 21
quiz:
  - question: តើ keyword មួយណាដែលត្រូវប្រើនៅពីមុខការហៅ function ដែលត្រឡប់មកវិញនូវ Promise ដើម្បីផ្អាកដំណើរការរហូតដល់ Promise នោះទទួលបានជោគជ័យ (resolves)?
    options:
      - async
      - await
      - then
      - catch
    correctAnswer: 1
    explanation: "Keyword `await` ផ្អាកដំណើរការរបស់ `async` function រហូតដល់ Promise ត្រូវបាន resolved ឬ rejected ដែលធ្វើអោយ asynchronous កូដមានដំណើរការស្រដៀងនឹង synchronous កូដ។"
---

## 🌐 ការទាញយកទិន្នន័យដោយប្រើ Fetch API

ឥឡូវនេះយើងបានយល់ពី `async` និង `await` រួចហើយ តោះយើងយកវាទៅអនុវត្តជាក់ស្តែងជាមួយការទាញយកទិន្នន័យ!

**Fetch API** គឺជាមុខងារស្រាប់របស់ Browser ដែលអនុញ្ញាតអោយយើងទាញយកទិន្នន័យពី URL ឬ API ណាមួយ។ ការហៅ `fetch()` តែងតែត្រឡប់មកវិញនូវ Promise។

**ឧទាហរណ៍៖ ទាញយកបញ្ជីឈ្មោះពី Fake API**
```javascript
const fetchUsers = async () => {
  try {
    // 1. ស្នើសុំទិន្នន័យពី API
    const response = await fetch("https://jsonplaceholder.typicode.com/users");
    
    // 2. បំប្លែងទិន្នន័យទៅជាទម្រង់ JSON
    const data = await response.json();
    
    // 3. បង្ហាញទិន្នន័យ
    console.log(data); // នឹងបង្ហាញ Array នៃ Users នៅក្នុង Console
  } catch (error) {
    // ចាប់យក Error ប្រសិនបើមានបញ្ហា (ឧទាហរណ៍៖ ដាច់អ៊ីនធឺណិត)
    console.error("មានបញ្ហាក្នុងការទាញយកទិន្នន័យ:", error);
  }
};

fetchUsers();
```

---

## 4. អនុវត្តជាក់ស្តែង៖ Fetch ទិន្នន័យ រួចបង្ហាញលើ DOM

នេះគឺជាឧទាហរណ៍ដ៏ពេញលេញមួយ ដែលរួមបញ្ចូលការ **Fetch API** (មេរៀនទី៦), **Array Methods** (`.map()`, មេរៀនទី៣), និង **DOM Update** (មេរៀនទី៥) បញ្ចូលគ្នា!

**HTML:**
```html
<button id="load-btn">ទាញយកអ្នកប្រើប្រាស់</button>
<ul id="user-list"></ul>
```

**JavaScript:**
```javascript
const btn = document.querySelector("#load-btn");
const userList = document.querySelector("#user-list");

// មុខងារសម្រាប់បង្ហាញទិន្នន័យលើអេក្រង់
const renderUsers = (users) => {
  // លុបអ្វីៗចាស់ចោលសិន
  userList.innerHTML = "";
  
  // ប្រើ .map() ដើម្បីឆ្លងកាត់ User ម្នាក់ៗ
  users.map(user => {
    const li = document.createElement("li"); // បង្កើត Node ថ្មី
    li.textContent = `${user.name} (${user.email})`; // បញ្ចូលខ្លឹមសារ
    userList.appendChild(li); // បន្ថែមចូលទៅក្នុង DOM
  });
};

// មុខងារសម្រាប់ Fetch ទិន្នន័យ
const handleFetch = async () => {
  btn.textContent = "កំពុងទាញយក...";
  
  try {
    const response = await fetch("https://jsonplaceholder.typicode.com/users");
    const data = await response.json();
    
    renderUsers(data); // យក Data ទៅបង្ហាញ
  } catch (error) {
    console.error("Error:", error);
  } finally {
    btn.textContent = "ទាញយកអ្នកប្រើប្រាស់"; // ប្តូរអក្សរប៊ូតុងមកដើមវិញ
  }
};

// បញ្ជាអោយប៊ូតុងដំណើរការ
btn.addEventListener("click", handleFetch);
```

**អបអរសាទរ! 🎉** 
អ្នកបានបញ្ចប់ម៉ូឌុល JavaScript ដោយជោគជ័យ! ការយល់ដឹងពីរបៀបភ្ជាប់ API និងផ្លាស់ប្តូរ DOM គឺជាជំនាញស្នូលបំផុតសម្រាប់ការក្លាយជា Frontend Developer!
