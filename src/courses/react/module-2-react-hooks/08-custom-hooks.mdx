---
title: "Custom Hooks: Extracting & Sharing Stateful Logic"
description: "ស្វែងយល់ពីរបៀបបង្កើត Custom Hooks ក្នុង React — វិធានការនៃការដាក់ឈ្មោះ use..., ការស្រង់ Stateful Logic ដែលប្រើដដែលៗ, និងការបង្កើត useLocalStorage, useDebounce។"
objectives:
  - "យល់ដឹងពីគោលការណ៍ Rules of Hooks និងការបង្កើត Custom Hook"
  - "ចេះស្រង់ Reusable Stateful Logic ចេញពី UI Components"
  - "ចេះបង្កើត Custom Hook ពេញនិយមដូចជា useLocalStorage និង useDebounce"
  - "ចេះរៀបចំ Folder រចនាសម្ព័ន្ធសម្រាប់ Custom Hooks"
module: module-2-react-hooks
order: 8
quiz:
  - question: "តើ Custom Hook ក្នុង React ត្រូវតែចាប់ផ្តើមដោយពាក្យគន្លឹះអ្វី?"
    options:
      - "`use` (ឧ. useAuth, useFetch, useWindowSize)"
      - "`get`"
      - "`handle`"
      - "`custom`"
    correctAnswer: 1
    explanation: "Custom Hook ត្រូវតែចាប់ផ្តើមដោយពាក្យ `use` ដើម្បីឱ្យ React Linter ដឹងថាវាជា Hook និងអាចអនុវត្តវិធានការ Rules of Hooks បាន។"
---

# 🪝 Building Custom React Hooks

**Custom Hook** គឺជា JavaScript Function ធម្មតាដែលចាប់ផ្តើមដោយពាក្យ `use` ហើយអាចហៅ React Hooks ផ្សេងទៀត (`useState`, `useEffect`, `useRef`) នៅខាងក្នុងខ្លួនបាន។

```mermaid
flowchart TD
    CustomHook["Custom Hook (useLocalStorage)"]
    CompA["Component A (<ThemeToggle />)"]
    CompB["Component B (<UserSettings />)"]

    CustomHook -. "Logic Reuse (Independent State)" .-> CompA
    CustomHook -. "Logic Reuse (Independent State)" .-> CompB
```

---

## ការបង្កើត `useDebounce` Hook (សម្រាប់ Live Search)

```tsx
// src/hooks/useDebounce.ts
import { useState, useEffect } from "react";

export function useDebounce<T>(value: T, delay: number = 500): T {
  const [debouncedValue, setDebouncedValue] = useState<T>(value);

  useEffect(() => {
    const timer = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => {
      clearTimeout(timer);
    };
  }, [value, delay]);

  return debouncedValue;
}
```
