---
title: "useReducer: Complex State Logic & Predictable Transitions"
description: "ស្វែងយល់ពី useReducer Hook — Reducer Function, Action Objects, Dispatch, State Transitions ដែលអាចទស្សន៍ទាយបាន, និងពេលណាដែលគួរប្រើ useReducer ជំនួស useState។"
objectives:
  - "យល់ច្បាស់ពីទម្រង់ Reducer Function: (state, action) => newState"
  - "ចេះបង្កើត Type-safe Actions ជាមួយ TypeScript Discriminated Unions"
  - "ចេះប្រើ dispatch({ type, payload }) ដើម្បីកេះ State Transitions"
  - "ដឹងពីលក្ខខណ្ឌដែលត្រូវជ្រើសរើសរវាង useReducer និង useState"
module: module-2-react-hooks
order: 5
quiz:
  - question: "តើនៅពេលណាដែលយើងគួរពិចារណាប្រើ `useReducer` ជំនួសឱ្យ `useState`?"
    options:
      - "នៅពេល State មានទម្រង់ស្មុគស្មាញ (Complex Nested Object) ឬមាន State Transitions ជាច្រើនដែលអាស្រ័យគ្នាទៅវិញទៅមក"
      - "នៅពេលចង់សរសេរកូដឱ្យខ្លីបំផុត"
      - "នៅពេលមានតែ String មួយ"
      - "នៅពេលមិនចង់ប្រើ React"
    correctAnswer: 1
    explanation: "`useReducer` ស័ក្តិសមបំផុតសម្រាប់ Complex State Logic ដែលមានសកម្មភាពច្រើន (Actions) ដូចជា Shopping Cart ឬ Form Wizard។"
---

# 🗃️ useReducer: Managing Complex State

`useReducer` គឺជាជម្រើសដ៏មានឥទ្ធិពលជំនួសឱ្យ `useState` នៅពេលដែលអ្នកមាន **Complex State Logic** ដែលមាន Actions និង State Transitions ច្រើន។

```mermaid
flowchart LR
    Component["React Component (UI)"]
    Action["Action Object<br/><code>{ type: 'ADD_ITEM', payload: item }</code>"]
    Reducer["Reducer Function<br/><code>(state, action) => newState</code>"]
    NewState["New Immutable State"]

    Component -->|"dispatch(action)"| Action
    Action --> Reducer
    Reducer --> NewState
    NewState -->|"Re-render"| Component
```

---

## ឧទាហរណ៍ Shopping Cart ជាមួយ `useReducer`

```tsx
import { useReducer } from "react";

interface CartItem {
  id: string;
  name: string;
  price: number;
  quantity: number;
}

interface CartState {
  items: CartItem[];
}

type CartAction =
  | { type: "ADD_ITEM"; payload: { id: string; name: string; price: number } }
  | { type: "REMOVE_ITEM"; payload: { id: string } }
  | { type: "CLEAR_CART" };

function cartReducer(state: CartState, action: CartAction): CartState {
  switch (action.type) {
    case "ADD_ITEM": {
      const existing = state.items.find((i) => i.id === action.payload.id);
      if (existing) {
        return {
          ...state,
          items: state.items.map((i) =>
            i.id === action.payload.id ? { ...i, quantity: i.quantity + 1 } : i
          ),
        };
      }
      return { ...state, items: [...state.items, { ...action.payload, quantity: 1 }] };
    }
    case "REMOVE_ITEM":
      return { ...state, items: state.items.filter((i) => i.id !== action.payload.id) };
    case "CLEAR_CART":
      return { items: [] };
    default:
      return state;
  }
}

export function ShoppingCart() {
  const [state, dispatch] = useReducer(cartReducer, { items: [] });

  return (
    <div className="p-6 bg-slate-900 rounded-2xl text-white space-y-4">
      <h2 className="text-xl font-bold">កន្ត្រកទំនិញ ({state.items.length})</h2>
      <button
        onClick={() => dispatch({ type: "ADD_ITEM", payload: { id: "p-1", name: "Keyboard", price: 80 } })}
        className="px-4 py-2 bg-indigo-600 rounded-lg"
      >
        + ថែម Keyboard ($80)
      </button>
      <button
        onClick={() => dispatch({ type: "CLEAR_CART" })}
        className="ml-2 px-4 py-2 bg-rose-600 rounded-lg"
      >
        សម្អាតកន្ត្រក
      </button>
    </div>
  );
}
```
