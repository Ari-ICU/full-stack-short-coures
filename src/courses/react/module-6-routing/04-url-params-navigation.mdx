---
title: "Dynamic Routes & URL Parameters — useParams & useNavigate"
description: សិក្សាពីការបង្កើត Dynamic Routes ជាមួយ URL Parameters (`:id`) — ចាប់ផ្តើមពីរចនាសម្ព័ន្ធ Normal HTML Query URL សិន រួចបំប្លែងទៅជា React Router `useParams` និង Programmatic Navigation `useNavigate` ជាមួយការពន្យល់មួយជួរៗ។
objectives:
  - ស្គាល់រចនាសម្ព័ន្ធ Normal HTML URLs ជាមួយ Dynamic IDs
  - ចេះកំណត់ Dynamic Segment Path (ឧ. `/products/:id`)
  - ចេះទាញយក URL Parameters ដោយប្រើ `useParams()` Hook
  - ចេះបញ្ជា Navigation តាម Program (Code) ដោយប្រើ `useNavigate()`
module: module-6-routing
order: 4
quiz:
  - question: "តើ `useParams()` ត្រឡប់ទិន្នន័យជាប្រភេទអ្វី?"
    options:
      - "JavaScript Object ដែលមាន Keys ត្រូវគ្នានឹងឈ្មោះ Dynamic Segments ក្នុង Path (ឧ. { id: '123' })"
      - "JavaScript Array"
      - "String តែមួយ"
      - "Boolean"
    correctAnswer: 1
    explanation: "useParams() ផ្តល់ Object មួយដែលផ្ទុក key-value pairs នៃ dynamic parameters ពី URL (ឧ. /users/:userId -> { userId: '42' })។"
---

## 📍 1. Dynamic Route Matching

```mermaid
graph LR
    URL["Browser URL: /courses/react-101"] --> Router["React Router Pattern: /courses/:courseSlug"]
    Router --> Hook["useParams() -> { courseSlug: 'react-101' }"]
    Hook --> Comp["CourseDetailPage Component"]
```

---

## 🔗 2. URL Parameters in Code

### 🌐 Normal HTML Links:

```html
<!-- HTML Product Links with Dynamic IDs -->
<a href="/products/1">iPhone 15</a>
<a href="/products/2">MacBook Pro</a>
```

### 💻 React Router Code:

```jsx
// src/pages/ProductDetail.jsx
import { useParams, useNavigate } from 'react-router-dom';

export function ProductDetail() {
  // 1. ទាញយក dynamic parameter ':id' ពី URL
  const { id } = useParams();
  
  // 2. Navigation Hook
  const navigate = useNavigate();

  return (
    <div className="p-4 bg-white dark:bg-slate-800 rounded-2xl shadow">
      <h3 className="font-bold">📱 ព័ត៌មានលម្អិតទំនិញ ID: #{id}</h3>
      <button
        onClick={() => navigate('/products')} // Programmatic Navigation
        className="mt-3 px-3 py-1 bg-slate-100 dark:bg-slate-700 text-xs rounded font-bold"
      >
        ← ត្រឡប់ក្រោយ
      </button>
    </div>
  );
}
```

### 🔍 ការពន្យល់មួយជួរៗ (Line-by-Line Explanation):

1. `const { id } = useParams();`
   - ទាញយកតម្លៃពី URL Path។ បើ User បើក `/products/42` នោះ `id` នឹងមានតម្លៃស្មើ `'42'`។
2. `const navigate = useNavigate();`
   - ផ្តល់ Function សម្រាប់ប្តូរទំព័រតាមរយៈ JavaScript Code (ដូចជា ក្រោយ submit form ឬចុចប៊ូតុង Back)។
3. `navigate('/products')`
   - នាំ User ត្រឡប់ទៅកាន់បញ្ជី Products វិញភ្លាមៗ។
