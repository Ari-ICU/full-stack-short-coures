---
title: Setting up React Router (react-router-dom)
description: សិក្សាពីការដំឡើង និងរៀបចំ Client-Side Routing ដោយប្រើប្រាស់ `react-router-dom` v6+ ក្នុង React។
objectives:
  - យល់ពី Client-Side Routing vs Server-Side Routing
  - ដំឡើងកញ្ចប់បណ្ណាល័យ `react-router-dom`
  - ចេះរៀបចំ `BrowserRouter`, `Routes`, និង `Route` components
module: module-6-routing
order: 1
quiz:
  - question: "តើអ្វីជាតួនាទីរបស់ `BrowserRouter` នៅក្នុង React Router?"
    options:
      - "ជា CSS framework សម្រាប់រចនា Layout"
      - "ជា Top-Level Provider ដែលតាមដាន Browser History API និង synchronize URL ជាមួយ React Component Tree"
      - "ជា Server សម្រាប់ Host វេបសាយ"
      - "ជា Database សម្រាប់ផ្ទុកទិន្នន័យ"
    correctAnswer: 2
    explanation: "`BrowserRouter` ប្រើប្រាស់ HTML5 History API (`pushState`, `replaceState`) ដើម្បីអនុញ្ញាតឱ្យ React ធ្វើការផ្លាស់ប្តូរ URL ដោយមិនបាច់ Refresh Page ឡើងវិញឡើយ។"
---

## 🗺️ 1. Client-Side Routing Architecture

```mermaid
flowchart TD
    subgraph ClientSideNavigation["Client-Side Route Matching Pipeline"]
        direction TB
        URL["Browser URL Changes: '/dashboard'"] --> Router["BrowserRouter (HTML5 History API)"]
        Router --> Match{"Route Matching Engine: path === '/dashboard'?"}
        Match -->|Match Found| Render["Mount & Render &lt;Dashboard /&gt; Component 🚀 (No Reload)"]
        Match -->|No Match| NotFound["Render 404 &lt;NotFound /&gt; Component"]
    end

    style ClientSideNavigation fill:#f8f9fa,stroke:#ced4da,color:#000
    style URL fill:#ebf8ff,stroke:#3182ce,color:#000
    style Render fill:#f0fff4,stroke:#38a169,color:#000
    style NotFound fill:#fff5f5,stroke:#e53e3e,color:#000
```

---

## ⚙️ 2. ការដំឡើង និង Setup

```bash
npm install react-router-dom
```

```jsx
// ឯកសារ: src/App.jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';

function Home() { return <h1 className="p-8 text-2xl font-bold">🏠 ទំព័រដើម (Home)</h1>; }
function About() { return <h1 className="p-8 text-2xl font-bold">ℹ️ អំពីយើង (About)</h1>; }
function NotFound() { return <h1 className="p-8 text-2xl font-bold text-red-500">404 រកមិនឃើញទំព័រឡើយ!</h1>; }

export default function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="*" element={<NotFound />} />
      </Routes>
    </BrowserRouter>
  );
}
```
