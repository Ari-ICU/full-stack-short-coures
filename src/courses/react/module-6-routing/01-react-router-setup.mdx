---
title: "React Router v6 Setup & Core Concepts"
description: សិក្សាពី Client-Side Routing ក្នុង React — ចាប់ផ្តើមពីរចនាសម្ព័ន្ធ Normal HTML Multi-Page Navigation សិន រួចបំប្លែងទៅជា Single Page App Routing ជាមួយ BrowserRouter, Routes, និង Route ព្រមទាំងពន្យល់មួយជួរៗ។
objectives:
  - យល់ពីភាពខុសគ្នារវាង Traditional Multi-Page Apps (MPA) និង Single-Page Apps (SPA)
  - ស្គាល់រចនាសម្ព័ន្ធ Normal HTML `<a>` Navigation
  - ចេះដំឡើង និង Setup `BrowserRouter` ក្នុង React
  - ចេះកំណត់ Basic Routes ជាមួយ `<Routes>` និង `<Route>`
module: module-6-routing
order: 1
quiz:
  - question: "តើអ្វីជាអត្ថប្រយោជន៍ចម្បងនៃ Client-Side Routing (React Router) ធៀបនឹង Server-side Multi-Page Apps?"
    options:
      - "ការផ្លាស់ប្តូរទំព័រមិនបាច់ Reload អេក្រង់ទាំងមូលឡើងវិញឡើយ (No Full Page Refresh) ធ្វើឱ្យការរុករកមានល្បឿនលឿន និងរក្សា State បានល្អ"
      - "React Router លុប HTML ចោល"
      - "React Router ធ្វើឱ្យ CSS មានចលនា"
      - "ដំណើរការតែក្នុង Chrome Browser ប៉ុណ្ណោះ"
    correctAnswer: 1
    explanation: "Client-Side Routing ស្ទាក់ចាប់ការចុច Link រួច update URL ដោយប្រើ Browser History API និង Re-render តែ Component ណាដែលត្រូវបង្ហាញ ដោយមិន Reload HTML/CSS ឡើងវិញឡើយ។"
---

## 🗺️ 1. Multi-Page (MPA) vs Single-Page Routing (SPA)

```mermaid
graph TD
    subgraph MPA["Traditional MPA (Full Reload) ❌"]
        A1["Click <a href='/about'>"] --> A2["Browser sends HTTP Request to Server"]
        A2 --> A3["Server returns full HTML"]
        A3 --> A4["White screen flash & Full Reload 🔄"]
    end

    subgraph SPA["React Router SPA (Zero Reload) ✅"]
        B1["Click <Link to='/about'>"] --> B2["React Router intercepts click"]
        B2 --> B3["Updates URL using History API"]
        B3 --> B4["Instant Component Swap (0ms reload) ⚡"]
    end
```

---

## 🧭 2. HTML Links vs React Router Configuration

### 🌐 Normal HTML Links:

```html
<!-- HTML Multi-Page Navigation (Reloads the whole page!) -->
<nav>
  <a href="/">Home</a>
  <a href="/about">About</a>
  <a href="/contact">Contact</a>
</nav>
```

### 💻 React Router Setup Code:

```jsx
// src/App.jsx
import { BrowserRouter, Routes, Route, Link } from 'react-router-dom';

function Home() { return <h2>🏠 ទំព័រដើម (Home)</h2>; }
function About() { return <h2>ℹ️ អំពីយើង (About)</h2>; }
function Contact() { return <h2>📞 ទំនាក់ទំនង (Contact)</h2>; }

export default function App() {
  return (
    <BrowserRouter>
      {/* NAVIGATION BAR */}
      <nav className="flex gap-4 p-4 bg-slate-100 dark:bg-slate-800">
        <Link to="/" className="font-bold text-blue-600">Home</Link>
        <Link to="/about" className="font-bold text-blue-600">About</Link>
        <Link to="/contact" className="font-bold text-blue-600">Contact</Link>
      </nav>

      {/* ROUTE DEFINITIONS */}
      <main className="p-6">
        <Routes>
          <Route path="/" element={<Home />} />
          <Route path="/about" element={<About />} />
          <Route path="/contact" element={<Contact />} />
        </Routes>
      </main>
    </BrowserRouter>
  );
}
```

### 🔍 ការពន្យល់មួយជួរៗ (Line-by-Line Explanation):

1. `<BrowserRouter>`
   - ផ្តល់បរិបទ Routing និងតភ្ជាប់ React App ទៅនឹង Browser URL Address Bar។
2. `<Link to="/about">`
   - ជំនួស `<a href="...">`។ វាទប់ស្កាត់ការ Refresh ទំព័រ និងប្តូរ Component ភ្លាមៗក្នុងពេល 0ms។
3. `<Routes>`
   - ដើរតួជា Container ពិនិត្យមើល URL បច្ចុប្បន្ន រួចជ្រើសរើស `<Route>` ណាដែលត្រូវគ្នាមកបង្ហាញ។
4. `<Route path="/about" element={<About />} />`
   - ប្រសិនបើ URL គឺ `/about` វានឹង Render Component `<About />`។
