---
title: Creating Scalable Custom Context Providers (AuthContext, CartContext)
description: សិក្សាពី Best Practices ក្នុងការរៀបចំ Scalable Custom Context Providers ជាមួយ Custom Consumer Hooks (`useAuth`, `useCart`)។
objectives:
  - បង្កើត Dedicated Context Provider Components (ដូចជា `AuthProvider`, `CartProvider`)
  - បង្កើត Custom Hooks (`useAuth()`) អមជាមួយ Error Boundary Check (`throw new Error if outside Provider`)
  - រៀបចំ Clean Architecture សម្រាប់ Global State
module: module-7-global-state
order: 4
quiz:
  - question: "ហេតុអ្វីបានជាយើងគួរបង្កើត Custom Hook `useAuth()` ជំនួសឱ្យការហៅ `useContext(AuthContext)` ផ្ទាល់ក្នុងគ្រប់ file?"
    options:
      - "ដើម្បីឱ្យកូដខ្លី និងអាច throw Error ច្បាស់លាស់ ប្រសិនបើនរណាម្នាក់ភ្លេច wrap Component ដោយ `<AuthProvider>`"
      - "ដើម្បីឱ្យ Context ដំណើរការជាមួយ CSS"
      - "ដើម្បីលុបបំបាត់ JavaScript"
      - "ដើម្បី run លើ mobile"
    correctAnswer: 1
    explanation: "Custom Context Hook (ដូចជា `useAuth()`) ជួយ Encapsulate context import និងផ្តល់ Developer-friendly error message ភ្លាមៗ ប្រសិនបើ component ត្រូវហៅនៅក្រៅ Provider boundary។"
---

## 🏛️ 1. Scalable Provider Architecture Pattern

ជំនួសឱ្យការសរសេរកូដ Provider និង State ទាំងអស់ច្របូកច្របល់ក្នុង `App.jsx` យើងបំបែកវាជា **Dedicated Context Module** ក្នុង `src/context/`៖

```mermaid
flowchart TD
    subgraph ContextModule["Dedicated Context Module (src/context/AuthContext.jsx)"]
        direction TB
        Ctx["1. <code>createContext()</code> (Internal)"]
        Prov["2. <code>&lt;AuthProvider&gt;</code> (Exports Provider Component)"]
        Hook["3. <code>useAuth()</code> (Exports Custom Consumer Hook)"]
        Ctx --- Prov --- Hook
    end

    subgraph CleanApp["Clean App Usage"]
        direction TB
        AppWrap["<code>&lt;AuthProvider&gt;&lt;App /&gt;&lt;/AuthProvider&gt;</code>"]
        NavComp["<code>const { user, logout } = useAuth();</code>"]
    end

    ContextModule --> CleanApp

    style ContextModule fill:#ebf8ff,stroke:#3182ce,color:#000
    style CleanApp fill:#f0fff4,stroke:#38a169,color:#000
```

---

## 🛠️ 2. កូដគំរូ: `AuthContext.jsx`

```jsx
// ឯកសារ: src/context/AuthContext.jsx
import { createContext, useContext, useState } from 'react';

const AuthContext = createContext(null);

export function AuthProvider({ children }) {
  const [user, setUser] = useState(null);

  const login = (userData) => {
    setUser(userData);
    localStorage.setItem('user', JSON.stringify(userData));
  };

  const logout = () => {
    setUser(null);
    localStorage.removeItem('user');
  };

  return (
    <AuthContext.Provider value={{ user, login, logout, isAuthenticated: !!user }}>
      {children}
    </AuthContext.Provider>
  );
}

// Custom Hook ជាមួយ Safety Check
export function useAuth() {
  const context = useContext(AuthContext);
  if (!context) {
    throw new Error('useAuth must be used within an <AuthProvider>');
  }
  return context;
}
```
