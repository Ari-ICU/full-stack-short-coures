---
title: "Context API Setup & The Provider-Consumer Pattern"
description: សិក្សាពីការបង្កើត Context ជាមួយ `createContext`, `Provider`, និង `useContext` — ចាប់ផ្តើមពីរចនាសម្ព័ន្ធ Normal HTML សិន រួចបំប្លែងទៅជា Shared Global State និងពន្យល់មួយជួរៗ។
objectives:
  - ស្គាល់រចនាសម្ព័ន្ធ Normal HTML Navigation & Settings
  - ចេះបង្កើត Context Object ដោយប្រើ `createContext(defaultValue)`
  - ចេះ Wrap Component Tree ជាមួយ `<ThemeContext.Provider value={...}>`
  - ចេះ Consume Global State ក្នុង Deeply Nested Components ដោយ `useContext()`
module: module-7-global-state
order: 2
quiz:
  - question: "តើ ៣ ជំហានគ្រឹះក្នុងការប្រើប្រាស់ React Context API មានអ្វីខ្លះ?"
    options:
      - "1. createContext() -> 2. Wrap ជាមួយ <Context.Provider value={...}> -> 3. Consume ដោយ useContext(Context)"
      - "1. useState() -> 2. useEffect() -> 3. return"
      - "1. fetch() -> 2. parse() -> 3. save()"
      - "1. HTML -> 2. CSS -> 3. JS"
    correctAnswer: 1
    explanation: "Context API ដំណើរការតាម Provider-Consumer Pattern៖ 1. បង្កើត Context, 2. បញ្ជូនតម្លៃតាមរយៈ Provider Component នៅ Top-level, 3. ទាញយកតម្លៃតាម useContext() ក្នុង Child Components។"
---

## 🌐 1. Provider-Consumer Mental Model

```mermaid
graph TD
    Provider["<ThemeContext.Provider value={{ theme, toggleTheme }}>"]
    Provider --> CompA["Header Component"]
    Provider --> CompB["Sidebar Component"]
    CompB --> DeepChild["ThemeToggleSwitch (Calls useContext!) 🎯"]

    style Provider fill:#ebf8ff,stroke:#3182ce,color:#000
    style DeepChild fill:#f0fff4,stroke:#38a169,color:#000
```

---

## 💻 2. ThemeContext Setup Step by Step

### 💻 Code សម្រាប់បង្កើត `ThemeContext.jsx`:

```jsx
// src/context/ThemeContext.jsx
import { createContext, useContext, useState } from 'react';

// 1. បង្កើត Context
export const ThemeContext = createContext(null);

// 2. បង្កើត Custom Provider Component
export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');

  const toggleTheme = () => {
    setTheme((prev) => (prev === 'light' ? 'dark' : 'light'));
  };

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

// 3. Custom Hook ងាយស្រួល Consume
export function useTheme() {
  const context = useContext(ThemeContext);
  if (!context) {
    throw new Error('useTheme must be used within a ThemeProvider');
  }
  return context;
}
```

### 🔍 ការពន្យល់មួយជួរៗ (Line-by-Line Explanation):

1. `createContext(null)`
   - បង្កើត Context Container ថ្មីមួយ។
2. `<ThemeContext.Provider value={{ theme, toggleTheme }}>`
   - បញ្ជូន Object `{ theme, toggleTheme }` ទៅគ្រប់ Components ទាំងអស់ដែលស្ថិតក្រោម `{children}`។
3. `export function useTheme()`
   - បង្កើត Hook ជួយឱ្យ Child Components មិនបាច់សរសេរ `useContext(ThemeContext)` ដដែលៗឡើយ។

---

## 🚀 3. Complete Integrated Theme Switcher App

```jsx
import { ThemeProvider, useTheme } from './context/ThemeContext';

function Header() {
  const { theme, toggleTheme } = useTheme();

  return (
    <header className="p-4 border-b flex justify-between items-center">
      <h1 className="font-bold text-sm">🌐 Global Theme Context</h1>
      <button
        onClick={toggleTheme}
        className="px-3 py-1 bg-blue-600 text-white rounded-lg text-xs font-bold"
      >
        Toggle: {theme === 'light' ? '🌙 Dark' : '☀️ Light'}
      </button>
    </header>
  );
}

function Content() {
  const { theme } = useTheme();

  return (
    <div className={`p-6 rounded-2xl m-4 text-center ${theme === 'dark' ? 'bg-slate-900 text-white' : 'bg-slate-100 text-slate-800'}`}>
      <h3 className="font-black text-lg">Current Theme: {theme}</h3>
      <p className="text-xs opacity-75 mt-1">Component នេះអាន Theme ពី Context ដោយផ្ទាល់ 0 Prop Drilling!</p>
    </div>
  );
}

export default function App() {
  return (
    <ThemeProvider>
      <div className="max-w-md mx-auto bg-white dark:bg-slate-800 rounded-3xl shadow border overflow-hidden">
        <Header />
        <Content />
      </div>
    </ThemeProvider>
  );
}
```
