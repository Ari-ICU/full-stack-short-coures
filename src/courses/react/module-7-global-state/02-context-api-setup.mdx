---
title: "Context API — createContext, Provider & useContext Fundamentals"
description: សិក្សាស៊ីជម្រៅពី React Context API — ស្វែងយល់ពី createContext, Provider Pattern, useContext Hook, ការបង្កើត Safe Custom Hook, និងវិធីទប់ស្កាត់បញ្ហា Re-render លើ Global State។
objectives:
  - យល់ច្បាស់ពី Provider & Consumer Pattern ក្នុង React
  - ចេះបង្កើត Context Container ជាមួយ `createContext(defaultValue)` និងយល់ពីតួនាទីរបស់ Default Value
  - ចេះប្រើ `<Context.Provider value={...}>` ដើម្បីចែករំលែក State និង Functions ចុះទៅកូនៗ
  - ចេះប្រើ `useContext` Hook រួមជាមួយ Fail-Fast Safety Guard ក្នុង Custom Hook
  - យល់ពីបញ្ហា Re-render នៅពេលបញ្ជូន Object Reference ក្នុង `value` prop និងវិធីដោះស្រាយជាមួយ `useMemo`
module: module-7-global-state
order: 2
quiz:
  - question: "តើ ៣ ជំហានគ្រឹះក្នុងការបង្កើត និងប្រើប្រាស់ React Context API មានអ្វីខ្លះ?"
    options:
      - "1. createContext() -> 2. Wrap Component Tree ជាមួយ <Context.Provider value={...}> -> 3. ទាញយក State តាម useContext(Context)"
      - "1. useState() -> 2. useEffect() -> 3. return JSX"
      - "1. fetch() API -> 2. JSON.parse() -> 3. localStorage.setItem()"
      - "1. document.getElementById() -> 2. addEventListener() -> 3. innerHTML"
    correctAnswer: 1
    explanation: "Context API ដំណើរការតាម Provider-Consumer Pattern៖ 1. បង្កើត Context Container, 2. បញ្ជូនតម្លៃតាមរយៈ Provider Component នៅ Top-level, 3. ទាញយកតម្លៃតាម useContext() ក្នុង Child Components ណាមួយក៏បាន។"

  - question: "តើតម្លៃ `defaultValue` ដែលយើងបញ្ជូនទៅ `createContext(defaultValue)` ដំណើរការនៅពេលណា?"
    options:
      - "ដំណើរការតែនៅពេលដែល Component មួយហៅ `useContext` នៅក្រៅព្រំដែន `<Context.Provider>` ប៉ុណ្ណោះ"
      - "ដំណើរការជា State ដំបូងរបស់ Provider គ្រប់ពេល"
      - "ដំណើរការតែនៅពេល Browser ដាច់ Internet"
      - "ដំណើរការតែនៅពេល Build Production"
    correctAnswer: 1
    explanation: "`defaultValue` ក្នុង `createContext(defaultValue)` មិនមែនជា Initial State របស់ Provider ទេ ប៉ុន្តែវាជា Fallback Value ដែលនឹងត្រូវប្រើ ប្រសិនបើសមាសភាគមួយព្យាយាមអាន Context ដោយមិនត្រូវបានរុំព័ទ្ធដោយ Provider ខាងលើ។"

  - question: "ហេតុអ្វីបានជាការបញ្ជូន `value={{ theme, toggleTheme }}` ទៅកាន់ Provider ដោយគ្មាន `useMemo` អាចបង្កជាបញ្ហា Performance ក្នុង App ធំៗ?"
    options:
      - "ព្រោះរាល់ពេល Provider Re-render វានឹងបង្កើត Object Reference ថ្មីជានិច្ច ដែលបង្ខំឱ្យ Consumer Components ទាំងអស់ត្រូវ Re-render តាម ទោះបីទិន្នន័យមិនផ្លាស់ប្តូរក៏ដោយ"
      - "ព្រោះវាធ្វើឱ្យ JavaScript error មិនដំណើរការ"
      - "ព្រោះ Provider មិនអាចផ្ទុក Functions បានទេ"
      - "ព្រោះ React ហាមឃាត់ការប្រើ Object ក្នុង Context"
    correctAnswer: 1
    explanation: "នៅក្នុង JavaScript `{}` តែងតែបង្កើត New Object Reference ក្នុង Memory។ ប្រសិនបើ Parent Re-render តម្លៃ `value` នឹងក្លាយជា Object ថ្មី ធ្វើឱ្យ Consumers ទាំងអស់ដែល Subscribe ត្រូវ Re-render ឡើងវិញ។ ការប្រើ `useMemo` ឬការបំបែក Context ជួយដោះស្រាយបញ្ហានេះ។"
---

## 🌐 ១. គោលការណ៍ Provider & Consumer (Mental Model)

ដើម្បីយល់ពីដំណើរការនៃ Context API សូមស្រមៃមើល **«ប៉មផ្សាយសញ្ញា Wi-Fi»** 📶 នៅក្នុងអគារមួយ៖
- **Provider (ប៉មបញ្ជូនសញ្ញា):** ដាក់នៅជាន់លើគេនៃអគារ ហើយធ្វើការផ្សាយរលកសញ្ញា (State & Functions) ចេញមកក្រៅ។
- **Consumers (ទូរស័ព្ទ / កុំព្យូទ័រ):** គ្រប់ឧបករណ៍ដែលស្ថិតនៅក្នុងរង្វង់គ្របដណ្តប់នៃអគារនោះ អាចភ្ជាប់យកសញ្ញា Wi-Fi (`useContext`) មកប្រើប្រាស់បានភ្លាមៗ ដោយមិនចាំបាច់តខ្សែឆ្លងកាត់ជាន់នីមួយៗឡើយ!

```mermaid
graph TD
    Provider["<ThemeContext.Provider value={{ theme, toggleTheme }}> 📶 (ប៉មផ្សាយ State)"]
    Provider --> Layout["<AppLayout> (មិនបាច់ទទួល Props)"]
    Layout --> Sidebar["<Sidebar> (មិនបាច់ទទួល Props)"]
    Layout --> Content["<MainContent> (មិនបាច់ទទួល Props)"]
    Sidebar --> Switch["<ThemeToggleSwitch> (useTheme() ភ្ជាប់យកផ្ទាល់!) 🎯"]
    Content --> Card["<ThemedCard> (useTheme() ភ្ជាប់យកផ្ទាល់!) 🎯"]

    style Provider fill:#ebf8ff,stroke:#3182ce,stroke-width:2px
    style Switch fill:#f0fff4,stroke:#38a169,stroke-width:2px
    style Card fill:#f0fff4,stroke:#38a169,stroke-width:2px
    style Layout fill:#f8f9fa,stroke:#cbd5e1,stroke-width:1px
    style Sidebar fill:#f8f9fa,stroke:#cbd5e1,stroke-width:1px
    style Content fill:#f8f9fa,stroke:#cbd5e1,stroke-width:1px
```

---

## 🛠️ ២. សសរស្តម្ភទាំង ៣ នៃ Context API (The 3 Core Pillars)

```
[ជំហាន ១: createContext] ➔ [ជំហាន ២: Context.Provider] ➔ [ជំហាន ៣: useContext]
    (បង្កើតបំពង់បង្ហូរ)              (ផ្សាយទិន្នន័យ)                 (ទាញយកមកប្រើប្រាស់)
```

### ១. `createContext(defaultValue)`
បង្កើត Context Object មួយ។ `defaultValue` គឺសម្រាប់ជា **Fallback** ក្នុងករណី Component មួយហៅប្រើ Context ដោយមិនមាន Provider រុំព័ទ្ធពីលើ។ ជាទូទៅគេកំណត់វាជា `null` ដើម្បីងាយស្រួលឆែក Error។

### ២. `<Context.Provider value={currentValue}>`
ជា React Component ដែលរុំព័ទ្ធ Subtree នៃ Components របស់អ្នក។ រាល់ Consumer ទាំងអស់ដែលនៅខាងក្រោម Provider នេះ នឹងទទួលបានតម្លៃដែលបញ្ជូនតាមរយៈ `value` prop។

### ៣. `useContext(ContextObject)`
ជា React Hook ដែលអនុញ្ញាតឱ្យ Functional Component ណាមួយអាចអានតម្លៃបច្ចុប្បន្នពី Provider ដែលនៅជិតបំផុតពីខាងលើ។

---

## 💻 ៣. ការអនុវត្តជាក់ស្តែង ៖ បង្កើត `ThemeContext.jsx` មួយជំហានម្តងៗ

ដើម្បីឱ្យកូដមានរបៀបរៀបរយតាមស្តង់ដារ Clean Architecture យើងបំបែកការបង្កើត Context ជាជំហានៗ ដោយភ្ជាប់ Logic ជាមួយ HTML/JSX ជាក់ស្តែង ៖

### ជំហានទី ១ ៖ បង្កើត Context Container (`createContext`)
បង្កើត Data Channel សម្រាប់ផ្ទុក State ដោយកំណត់ Fallback Value ជា `null` ៖

```javascript
// src/context/ThemeContext.jsx
import { createContext } from 'react';

// ១. បង្កើត Context Container
export const ThemeContext = createContext(null);
```

---

### ជំហានទី ២ ៖ បង្កើត Provider Component (`ThemeProvider`)
គ្រប់គ្រង State ពិតប្រាកដ និងរុំព័ទ្ធ Child Components ដោយ `<ThemeContext.Provider>` ៖

```jsx
// src/context/ThemeContext.jsx
import { useState, useMemo } from 'react';

export function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light'); // 'light' | 'dark'

  // Function សម្រាប់ Toggle Theme
  const toggleTheme = () => {
    setTheme((prev) => (prev === 'light' ? 'dark' : 'light'));
  };

  // ✅ ការពារ Re-renders ដោយប្រើ useMemo
  const contextValue = useMemo(() => ({
    theme,
    isDark: theme === 'dark',
    toggleTheme,
  }), [theme]);

  // HTML / JSX Provider Wrapper
  return (
    <ThemeContext.Provider value={contextValue}>
      {children}
    </ThemeContext.Provider>
  );
}
```

---

### ជំហានទី ៣ ៖ បង្កើត Safe Custom Hook (`useTheme`)
បង្កើត Hook ផ្ទាល់ខ្លួនជាមួយ Fail-Fast Safety Guard ដើម្បីងាយស្រួលហៅប្រើក្នុង Components ៖

```javascript
// src/context/ThemeContext.jsx
import { useContext } from 'react';

export function useTheme() {
  const context = useContext(ThemeContext);

  // 🛡️ Error Safety Guard: ផ្តល់ដំណឹងភ្លាមៗបើភ្លេច Wrap Provider
  if (!context) {
    throw new Error('useTheme() ត្រូវតែប្រើនៅខាងក្នុង <ThemeProvider> ប៉ុណ្ណោះ!');
  }

  return context;
}
```

---

## 🚀 ៤. ការប្រើប្រាស់ Context ក្នុង Child Components & HTML

ខាងក្រោមនេះជារបៀបដែល Child Components ទាញយក State និង Actions មកភ្ជាប់ក្នុង HTML UI ៖

### ៤.១ — Theme Toggle Button (Header Component)
- **Logic & Hook:** ទាញយក `isDark` និង `toggleTheme`
- **HTML / JSX UI:**
```jsx
// src/components/Header.jsx
import { useTheme } from '../context/ThemeContext';

export function Header() {
  const { theme, isDark, toggleTheme } = useTheme();

  return (
    <header className="header">
      <h1>React Global Theme Switcher</h1>

      {/* ប៊ូតុងចុច Toggle Theme */}
      <button onClick={toggleTheme}>
        {isDark ? '☀️ ប្តូរទៅ Light Mode' : '🌙 ប្តូរទៅ Dark Mode'}
      </button>
    </header>
  );
}
```

---

### ៤.២ — Themed Content Card (ProfileCard Component)
- **Logic & Hook:** ទាញយក `theme` និង `isDark`
- **HTML / JSX UI:**
```jsx
// src/components/ProfileCard.jsx
import { useTheme } from '../context/ThemeContext';

export function ProfileCard() {
  const { theme, isDark } = useTheme();

  return (
    <div className={isDark ? 'card-dark' : 'card-light'}>
      <h2>John Doe</h2>
      <p>Senior React Engineer</p>

      {/* បង្ហាញស្ថានភាព Theme បច្ចុប្បន្ន */}
      <p>ស្ថានភាព Theme ៖ <strong>{theme}</strong></p>
    </div>
  );
}
```

---

### ៤.៣ — Wrap Root Component ដោយ `<ThemeProvider>`
ផ្គុំ Components ទាំងអស់បញ្ចូលគ្នាដោយរុំព័ទ្ធនៅ Top-Level ៖

```jsx
// src/App.jsx
import { ThemeProvider } from './context/ThemeContext';
import { Header } from './components/Header';
import { ProfileCard } from './components/ProfileCard';

export default function App() {
  return (
    <ThemeProvider>
      <div className="app-layout">
        <Header />
        <main>
          <ProfileCard />
        </main>
      </div>
    </ThemeProvider>
  );
}
```

---

## 🔍 ៥. ការពន្យល់កូដមួយជំហានៗ (Line-by-Line Breakdown)

1. `export const ThemeContext = createContext(null);`
   - បង្កើត Context Container។ យើងដាក់ `null` ជា default ដើម្បីឱ្យ Custom Hook អាចឆែកដឹងថា តើ component កំពុងរត់ក្នុង Provider ឬអត់។
2. `const [theme, setTheme] = useState('light');`
   - រក្សាទុក State ពិតប្រាកដនៅខាងក្នុង `ThemeProvider`។
3. `const contextValue = useMemo(() => ({ theme, isDark, toggleTheme }), [theme]);`
   - ចងក្រង State និង Handlers ចូលក្នុង Object តែមួយ។ ដោយសារមាន `useMemo` Object នេះនឹងមិនត្រូវបានបង្កើតឡើងវិញរាល់ពេល Parent component re-render ឡើយ ដរាបណា `theme` មិនទាន់ផ្លាស់ប្តូរ។
4. `if (!context) throw new Error(...)`
   - **Fail-Fast Error Handling:** បើ Developer ម្នាក់ភ្លេច wrap `<ThemeProvider>` នៅពីលើ ហើយព្យាយាមហៅ `useTheme()` កម្មវិធីនឹងផ្តល់សារព្រមានច្បាស់លាស់ភ្លាមៗ មិនបណ្តោយឱ្យកើត Bug ស្ងាត់ៗឡើយ។

---

## ⚡ ៦. ការយល់ដឹងពី Re-render Gotcha & Best Practices

> [!WARNING]
> **Context Re-render Pitfall ៖**  
> នៅពេលតម្លៃ `value` ក្នុង Provider ផ្លាស់ប្តូរ **រាល់ Component ទាំងអស់ដែលហៅ `useContext` នឹងត្រូវ Re-render ឡើងវិញដោយស្វ័យប្រវត្តិ** ទោះបីជា Component នោះប្រើប្រាស់តែមួយចំណែកតូចនៃ Data ក៏ដោយ!

### 💡 ដំណោះស្រាយដើម្បីបង្កើន Performance:
1. **កុំដាក់ State ទាំងអស់ចូលក្នុង Context តែមួយ:** បំបែកជា Contexts តូចៗតាមមុខងារ (ឧ. `AuthContext`, `ThemeContext`, `CartContext`)។
2. **បំបែក State និង Dispatch (Actions):** បង្កើត `ThemeStateContext` សម្រាប់តែទិន្នន័យ និង `ThemeDispatchContext` សម្រាប់តែ Functions ដូចជា `toggleTheme`។ វិធីនេះ Components ដែលត្រូវការតែប៊ូតុងចុច នឹងមិនចាំបាច់ Re-render ពេល State ផ្លាស់ប្តូរឡើយ!
3. **ប្រើ `useMemo` សម្រាប់ Object Value:** តែងតែរុំ Object នៃ `value` prop ដោយ `useMemo` ជានិច្ច។

---

## 🧠 ៧. សង្ខេប Mental Model ងាយចាំ (Quick Reference)

| ធាតុផ្សំ | កូដសរសេរ | តួនាទីជាក់ស្តែង |
| :--- | :--- | :--- |
| **Context Instance** | `const Ctx = createContext(null)` | បំពង់បង្ហូរទិន្នន័យ (Data Channel) |
| **Provider** | `<Ctx.Provider value={value}>` | អ្នកផ្សាយ និងគ្រប់គ្រង State ទៅកាន់ Subtree |
| **Consumer Hook** | `const data = useContext(Ctx)` | អ្នកចាប់សញ្ញា និង Subscribe យក State |
| **Custom Hook Guard** | `export function useMyCtx() { ... }` | ផ្តល់ Developer Experience ល្អ និងសុវត្ថិភាពខ្ពស់ |
