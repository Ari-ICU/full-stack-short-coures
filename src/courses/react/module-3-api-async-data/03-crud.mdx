---
title: "CRUD: Full RESTful Operations in React"
description: "ស្វែងយល់ពីការអនុវត្ត CRUD Operations ពេញលេញក្នុង React — Create (POST), Read (GET), Update (PUT/PATCH), Delete (DELETE), និងការ Sync State ជាមួយ Backend API។"
objectives:
  - "ចេះអនុវត្ត HTTP Methods: GET, POST, PUT, PATCH, DELETE"
  - "ចេះ Update React Local State ភ្លាមៗក្រោយពេល API Success"
  - "យល់ដឹងពីភាពខុសគ្នារវាង Optimistic UI Updates និង Pessimistic Updates"
  - "គ្រប់គ្រង Action Confirmation Modals មុនពេល Delete"
module: module-3-api-async-data
order: 3
quiz:
  - question: "តើ Optimistic UI Update មានន័យដូចម្តេច?"
    options:
      - "ការ Update UI លើ Screen ភ្លាមៗមុនពេល Server ឆ្លើយតប ហើយបើ Server Error ទើប Rollback មកវិញ"
      - "ការរង់ចាំ Server ឆ្លើយតបរយៈពេល ១០ នាទី"
      - "ការលុប UI ទាំងអស់ចោល"
      - "ការ Update UI តែនៅពេលថ្ងៃត្រង់"
    correctAnswer: 1
    explanation: "Optimistic UI ផ្តល់នូវ Fast UX ដោយធ្វើបច្ចុប្បន្នភាព UI ភ្លាមៗ ហើយប្រសិនបើ Network Error ទើប Revert ទិន្នន័យមកសភាពដើមវិញ។"
---

# 🔁 RESTful CRUD Operations

CRUD តំណាងឱ្យ **Create**, **Read**, **Update**, និង **Delete** ដែលជាប្រតិបត្តិការស្នូលក្នុង Web Application ភាគច្រើន។

```mermaid
flowchart TD
    UI["React UI (Buttons & Forms)"]
    POST["POST /api/posts (Create)"]
    GET["GET /api/posts (Read)"]
    PUT["PUT /api/posts/:id (Update)"]
    DELETE["DELETE /api/posts/:id (Delete)"]
    State["React State (setPosts)"]

    UI --> POST --> State
    UI --> GET --> State
    UI --> PUT --> State
    UI --> DELETE --> State
```

---

## ឧទាហរណ៍៖ CRUD Service & Component Integration

```tsx
import { useState } from "react";
import { apiClient } from "../services/api";

interface Post {
  id: string;
  title: string;
  body: string;
}

export function PostManager() {
  const [posts, setPosts] = useState<Post[]>([]);

  // CREATE (POST)
  const handleCreate = async (title: string, body: string) => {
    const res = await apiClient.post<Post>("/posts", { title, body });
    setPosts((prev) => [res.data, ...prev]);
  };

  // UPDATE (PUT)
  const handleUpdate = async (id: string, updatedTitle: string) => {
    const res = await apiClient.patch<Post>(`/posts/${id}`, { title: updatedTitle });
    setPosts((prev) => prev.map((p) => (p.id === id ? res.data : p)));
  };

  // DELETE (DELETE)
  const handleDelete = async (id: string) => {
    if (!window.confirm("តើអ្នកពិតជាចង់លុបមែនទេ?")) return;
    await apiClient.delete(`/posts/${id}`);
    setPosts((prev) => prev.filter((p) => p.id !== id));
  };

  return <div className="space-y-4">{/* Render Form & List */}</div>;
}
```
