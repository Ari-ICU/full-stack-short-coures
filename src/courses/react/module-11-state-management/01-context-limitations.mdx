---
title: "Context API Limitations & The Need for Modern External State Managers"
description: ស្វែងយល់ពីដែនកំណត់នៃ React Context API ក្នុងកម្មវិធីខ្នាតធំ — បញ្ហា Unnecessary Re-render Waterfall ដោយសារគ្មាន Selective Subscription, Provider Hell, និងមូលហេតុដែលយើងត្រូវប្រើប្រាស់ External State Manager ដូចជា Zustand។
objectives:
  - "យល់ច្បាស់ពី Context Re-render Problem ក្នុងការចែករំលែក Global State ដែលមានការផ្លាស់ប្តូរញឹកញាប់"
  - "ដឹងពីមូលហេតុដែល Context API មិនគាំទ្រ Selective Subscription តាមលំនាំដើម"
  - "ស្គាល់ពីបញ្ហា 'Provider Hell' និងភាពស្មុគស្មាញនៃ Boilerplate Code ក្នុង Context API"
  - "យល់ច្បាស់ពី State Management Decision Matrix (ពេលណាត្រូវប្រើ useState, Context API, Zustand, ឬ TanStack Query)"
module: module-11-state-management
order: 1
quiz:
  - question: "តើអ្វីទៅជា 'Context Re-render Problem' នៅក្នុង React នៅពេល App រីកធំឡើង?"
    options:
      - "នៅពេលដែល Field តែមួយក្នុង Context Object ផ្លាស់ប្តូរ (ឧ. Cart Items) នោះគ្រប់ Components ទាំងអស់ដែលហៅ `useContext` នឹងត្រូវ Re-render ឡើងវិញទាំងអស់ ទោះបីជា Component នោះប្រើប្រាស់តែ User Profile ឬ Theme ក៏ដោយ"
      - "Context API ដំណើរការបានតែនៅលើ Server-side ប៉ុណ្ណោះ"
      - "Context API បណ្តាលឱ្យកូដ JavaScript បាត់បង់ Type Safety"
      - "Context API មិនអាចផ្ទុក Objects ឬ Arrays បានទេ"
    correctAnswer: 1
    explanation: "React Context API មិនមានយន្តការ Selective Subscription ឡើយ។ នៅពេល `value` ក្នុង Provider ទទួលបាន Object Reference ថ្មី រាល់ Consumer ទាំងអស់ដែល Subscribe លើ Context នោះនឹងត្រូវបង្ខំឱ្យ Re-render ទាំងអស់ ដែលនាំឱ្យ UI ទាក់ (Lag) ក្នុង Large-scale Applications។"

  - question: "តើ Zustand ជួយដោះស្រាយបញ្ហា Re-render របស់ Context API តាមរបៀបណា?"
    options:
      - "Zustand ប្រើប្រាស់ 'Granular Selectors' (ឧ. `useStore(state => state.user)`) ដែលធានាថា Component នឹង Re-render លុះត្រាតែ Field ជាក់លាក់ដែលវាបានជ្រើសរើសមានការផ្លាស់ប្តូរប៉ុណ្ណោះ"
      - "Zustand លុបចោល React Virtual DOM ទាំងស្រុង"
      - "Zustand បង្ខំឱ្យ Component ទាំងអស់ក្លាយជា Static HTML"
      - "Zustand ដំណើរការតែលើ Mobile App ប៉ុណ្ណោះ"
    correctAnswer: 1
    explanation: "Zustand ដំណើរការក្រៅ React Component Tree (External Store) និងគាំទ្រ Granular Selectors។ ប្រសិនបើ Cart ផ្លាស់ប្តូរ មានតែ Cart Component ប៉ុណ្ណោះដែល Re-render ចំណែក User Profile និង Navbar នឹងមិន Re-render ឡើយ។"

  - question: "តើក្នុងករណីណាដែល React Context API នៅតែជាជម្រើសដ៏ល្អបំផុត?"
    options:
      - "សម្រាប់ Low-Frequency State (ទិន្នន័យដែលកម្រផ្លាស់ប្តូរ) ដូចជា Theme Mode (Light/Dark), Multi-Language (km/en), ឬ Current Authenticated User"
      - "សម្រាប់ Real-time Chat Apps ដែលមានសាររាប់ពាន់ចូលមកក្នុង ១ វិនាទី"
      - "សម្រាប់ Animation លើ Canvas"
      - "សម្រាប់ Server Data Caching"
    correctAnswer: 1
    explanation: "Context API ត្រូវបានរចនាឡើងយ៉ាងល្អឥតខ្ចោះសម្រាប់ Static ឬ Low-frequency State (Theme, Locale, Auth Session)។ ចំណែកឯ High-frequency State (Form State, Cart, Real-time Feeds) គួរតែប្រើ Zustand ឬ TanStack Query។"
---

## 💥 ១. The Context Re-render Problem (បញ្ហាដែលត្រូវដោះស្រាយ)

React Context API គឺជាឧបករណ៍ដ៏អស្ចារ្យសម្រាប់ចៀសវាង Prop Drilling។ ក៏ប៉ុន្តែ នៅពេលដែល Application របស់អ្នករីកធំឡើង Context API មានដែនកំណត់ធំមួយ ៖  
**«Context API មិនមានយន្តការ Selective Subscription ឡើយ!»**

ប្រសិនបើអ្នកបង្កើត Context ធំមួយ `{ user, theme, cart, notifications }` ៖  
រាល់ពេលដែល User ចុច *Add to Cart* ➔ តម្លៃ `cart` ផ្លាស់ប្តូរ ➔ **គ្រប់ Components ទាំងអស់ដែលអាន Context នេះ (រួមទាំង UserProfile, Sidebar, ThemeToggle) នឹងត្រូវ Re-render ទាំងអស់!**

```mermaid
graph TD
    subgraph ContextProblem["❌ Context API: Re-render Cascade (ខ្ជះខ្ជាយ CPU)"]
        direction TB
        CTX["<GlobalContext.Provider value={{ user, theme, cart }}>"]
        CTX --> C1["<UserProfile> (ប្រើតែ user) 💥 Re-renders!"]
        CTX --> C2["<Sidebar> (ប្រើតែ theme) 💥 Re-renders!"]
        CTX --> C3["<CartBadge> (ប្រើតែ cart) ✅ Re-renders!"]
    end

    subgraph ZustandSolution["✅ Zustand: Granular Selective Subscriptions"]
        direction TB
        Store[("External Zustand Store\n{ user, theme, cart }")]
        Store -.->|"useStore(s => s.user)"| Z1["<UserProfile> ⚡ (Skip Render)"]
        Store -.->|"useStore(s => s.theme)"| Z2["<Sidebar> ⚡ (Skip Render)"]
        Store ==>|"useStore(s => s.cart)"| Z3["<CartBadge> 🔄 (Re-renders alone!)"]
    end

    style ContextProblem fill:#fff5f5,stroke:#e53e3e,stroke-width:2px
    style ZustandSolution fill:#f0fff4,stroke:#38a169,stroke-width:2px
    style Z1 fill:#c6f6d5,stroke:#38a169,stroke-width:2px
    style Z2 fill:#c6f6d5,stroke:#38a169,stroke-width:2px
```

---

## 💻 ២. ការបង្ហាញបញ្ហាក្នុងកូដជាក់ស្តែង (Proof in Code)

### ❌ បញ្ហាក្នុង Context API:

```jsx
// 1. GlobalContext.jsx
const GlobalContext = createContext(null);

export function GlobalProvider({ children }) {
  const [user] = useState({ name: 'Sok Dara', role: 'Admin' });
  const [cart, setCart] = useState([]);

  const addToCart = (item) => setCart((prev) => [...prev, item]);

  return (
    <GlobalContext.Provider value={{ user, cart, addToCart }}>
      {children}
    </GlobalContext.Provider>
  );
}

// 2. UserProfile.jsx — Component នេះប្រើតែ 'user' មិនដែលប៉ះពាល់ 'cart' ឡើយ!
function UserProfile() {
  const { user } = useContext(GlobalContext);
  
  // ⚠️ នឹង Console.log រាល់ពេល User ចុច Add to Cart!
  console.log('🔴 UserProfile Re-rendered!'); 

  return <div>👤 {user.name} ({user.role})</div>;
}
```

### 💣 ហេតុអ្វីបានជា `UserProfile` Re-render?
1. ពេល `addToCart` ដំណើរការ `setCart` ធ្វើឱ្យ `GlobalProvider` Re-render។
2. Object ក្នុង `value={{ user, cart, addToCart }}` ទទួលបាន New Reference ក្នុង Memory។
3. React ពិនិត្យឃើញ `value` ប្រែប្រួល ទើបបញ្ជាឱ្យ **គ្រប់ Component ណាដែលហៅ `useContext(GlobalContext)` ត្រូវ Re-render ទាំងអស់** ដោយគ្មានការលើកលែង!

---

## ⚡ ៣. ដំណោះស្រាយដ៏មានឥទ្ធិពល ៖ Zustand Granular Selectors

នៅក្នុង Zustand យើងអាចជ្រើសរើសអានតែ Field ណាដែល Component ពិតជាត្រូវការ (Selective Subscription) ៖

```javascript
// 1. បង្កើត Zustand Store ក្រៅ React Tree
import { create } from 'zustand';

export const useStore = create((set) => ({
  user: { name: 'Sok Dara', role: 'Admin' },
  cart: [],
  addToCart: (item) => set((s) => ({ cart: [...s.cart, item] })),
}));
```

```jsx
// 2. UserProfile គ្រាន់តែ Subscribe លើ 'state.user' ប៉ុណ្ណោះ
function UserProfile() {
  const user = useStore((state) => state.user);

  // ✅ នឹងមិន Re-render ឡើយនៅពេល cart ផ្លាស់ប្តូរ!
  console.log('🟢 UserProfile Rendered');

  return <div>👤 {user.name}</div>;
}
```

---

## 🎯 ៤. State Management Decision Matrix (យុទ្ធសាស្ត្រជ្រើសរើស Tool)

| ប្រភេទ State (State Category) | ឧបករណ៍ដែលស័ក្តិសមបំផុត | ឧទាហរណ៍ជាក់ស្តែង |
| :--- | :--- | :--- |
| **Local Component State** | `useState` / `useReducer` | Form Input, Accordion Open/Close, Modal Toggle |
| **Low-Frequency Global State** | **React Context API** | Theme (Dark/Light), Language (km/en), Auth User Session |
| **High-Frequency Complex Client State** | **Zustand** | E-Commerce Cart, Canvas Editor, Music Player, Multi-step Wizard |
| **Server Cache State (Async API)** | **TanStack Query (React Query)** | Fetching API Lists, Auto-polling, Mutation, Pagination Caching |

---

## 🧠 ៥. សង្ខេប Mental Model ងាយចាំ

- **Context API** មិនមែនជា State Manager ទេ តែជា **Dependency Injection / Transport Mechanism** សម្រាប់បញ្ជូន Props ពីលើចុះក្រោមដោយគ្មាន Prop Drilling។
- **Zustand** គឺជា **True Central State Manager** ដែលមាន Selective Subscriptions, គ្មាន Provider Wrapper, និងមាន Performance ខ្ពស់បំផុត។
