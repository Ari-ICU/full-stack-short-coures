---
title: Fetching REST APIs using Fetch & Axios
description: សិក្សាពីរបៀបទាញយកទិន្នន័យពី RESTful API ដោយប្រើ Browser `fetch` API និងបណ្ណាល័យ `axios` ក្នុង React។
objectives:
  - ចេះធ្វើ GET & POST Requests ជាមួយ `fetch` API និង `async/await`
  - ចេះប្រើប្រាស់ `axios` សម្រាប់ HTTP Requests ទំនើប
  - យល់ពីរបៀបរៀបចំ Async Function ខាងក្នុង `useEffect`
module: module-4-effects-api
order: 3
quiz:
  - question: "ហេតុអ្វីបានជាយើងមិនអាចសរសេរ `useEffect(async () => {...})` ដោយផ្ទាល់?"
    options:
      - "ព្រោះ async function នឹង return Promise តែ `useEffect` តម្រូវឱ្យ callback return តែ Cleanup Function ឬ nothing (undefined) ប៉ុណ្ណោះ"
      - "ព្រោះ JavaScript មិនគាំទ្រ async/await"
      - "ព្រោះ fetch ដើរតែជាមួយ sync function"
      - "ព្រោះ Vite មិនអនុញ្ញាត"
    correctAnswer: 1
    explanation: "Effect Callback ត្រូវ return តែ cleanup function ប៉ុណ្ណោះ។ ដើម្បីប្រើ async/await យើងត្រូវបង្កើត async function មួយនៅខាងក្នុង callback នោះ រួច invoke វា (`const fetchData = async () => {...}; fetchData();`)។"
---

## 🌐 1. Data Fetching Architecture

```mermaid
flowchart LR
    A["React Component<br>(useEffect)"] -->|1. fetch() / axios.get()| B("REST API Server")
    B -->|2. Returns JSON Payload| A
    A -->|3. Update React State| C["Re-render with Live Data 🚀"]

    style A fill:#ebf8ff,stroke:#3182ce,color:#000
    style B fill:#f8f9fa,stroke:#ced4da,color:#000
    style C fill:#f0fff4,stroke:#38a169,color:#000
```

---

## 🛠️ 2. កូដគំរូ: `fetch` vs `axios`

### វិធីសាស្ត្រទី ១: ប្រើ Browser Native `fetch`
```jsx
import { useState, useEffect } from 'react';

export function UserListFetch() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    // បង្កើត async function នៅខាងក្នុង effect
    const loadUsers = async () => {
      try {
        const res = await fetch('https://jsonplaceholder.typicode.com/users');
        if (!res.ok) throw new Error('Network error!');
        const data = await res.json();
        setUsers(data);
      } catch (err) {
        console.error(err);
      }
    };

    loadUsers();
  }, []);

  return (
    <ul>
      {users.map((u) => <li key={u.id}>{u.name} ({u.email})</li>)}
    </ul>
  );
}
```

### វិធីសាស្ត្រទី ២: ប្រើ `axios` (Auto JSON Parsing & Simpler Syntax)
```bash
npm install axios
```

```jsx
import axios from 'axios';
import { useState, useEffect } from 'react';

export function UserListAxios() {
  const [users, setUsers] = useState([]);

  useEffect(() => {
    const loadUsers = async () => {
      try {
        // axios parse JSON ដោយស្វ័យប្រវត្តិក្នុង res.data
        const res = await axios.get('https://jsonplaceholder.typicode.com/users');
        setUsers(res.data);
      } catch (err) {
        console.error(err);
      }
    };

    loadUsers();
  }, []);

  return (
    <ul>
      {users.map((u) => <li key={u.id}>{u.name}</li>)}
    </ul>
  );
}
```
