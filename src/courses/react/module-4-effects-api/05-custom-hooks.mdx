---
title: Custom Hooks (Abstracting Reusable Fetch Logic)
description: សិក្សាពីរបៀបបង្កើត Custom Hook (`useFetch`) ដើម្បី encapsulate និង reuse Data Fetching Logic នៅគ្រប់ Components ទាំងអស់។
objectives:
  - យល់ពីគោលការណ៍ Rules of Hooks និង Custom Hooks
  - បង្កើត Custom Hook `useFetch(url)` ជាមួយ State & Cleanup logic
  - យល់ពីភាពដាច់ដោយឡែកគ្នានៃ State Instances ក្នុង Custom Hooks
module: module-4-effects-api
order: 5
quiz:
  - question: "តើ Component A និង Component B ដែលហៅ `useFetch()` ដូចគ្នា នឹង share state តែមួយរួមគ្នាដែរឬទេ?"
    options:
      - "បាទ/ចាស ពួកវា share state តែមួយដូច Global Store"
      - "ទេ! រាល់ការហៅ Custom Hook ម្តងៗ React នឹងបង្កើត State Instances ថ្មីដាច់ដោយឡែកពីគ្នា ១០០% សម្រាប់ Component នោះ"
      - "ពួកវា share state លុះត្រាតែមាន Context Provider"
      - "ដំណើរការតែលើទំព័រតែមួយ"
    correctAnswer: 2
    explanation: "Custom Hooks គឺជាការ Reuse Stateful Logic មិនមែន Reuse State Data ឡើយ។ Component នីមួយៗទទួលបាន `useState` និង `useEffect` ដាច់ដោយឡែកពីគ្នាទាំងស្រុង។"
---

## 🪢 1. Custom Hook Architecture

```mermaid
flowchart TD
    subgraph Encapsulation["Reusable Logic Layer: <code>useFetch(url)</code>"]
        direction TB
        Hook["Custom Hook: <code>useFetch</code>"]
        HookInternal["Encapsulates:<br>• <code>useState (data, loading, error)</code><br>• <code>useEffect (fetch &amp; abort cleanup)</code>"]
        Hook --- HookInternal
    end

    subgraph ConsumerComponents["Consumer Components (Isolated State Instances)"]
        direction LR
        CompA["&lt;ProductList /&gt;<br><code>const { data, loading } = useFetch('/api/products')</code><br>State Instance A (Independent)"]
        CompB["&lt;UserProfile /&gt;<br><code>const { data, loading } = useFetch('/api/user')</code><br>State Instance B (Independent)"]
    end

    Encapsulation -->|Reuse Logic| CompA
    Encapsulation -->|Reuse Logic| CompB

    style Encapsulation fill:#ebf8ff,stroke:#3182ce,color:#000
    style ConsumerComponents fill:#f8f9fa,stroke:#ced4da,color:#000
    style CompA fill:#f0fff4,stroke:#38a169,color:#000
    style CompB fill:#f0fff4,stroke:#38a169,color:#000
```

---

## 🛠️ 2. បង្កើត Reusable `useFetch` Hook

```javascript
// ឯកសារ: src/hooks/useFetch.js
import { useState, useEffect } from 'react';

export function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    let isMounted = true; // Flag ការពារ state update ពេល unmount
    setLoading(true);

    fetch(url)
      .then((res) => {
        if (!res.ok) throw new Error('Failed to fetch data!');
        return res.json();
      })
      .then((json) => {
        if (isMounted) {
          setData(json);
          setError(null);
        }
      })
      .catch((err) => {
        if (isMounted) setError(err.message);
      })
      .finally(() => {
        if (isMounted) setLoading(false);
      });

    return () => {
      isMounted = false; // Cleanup
    };
  }, [url]);

  return { data, loading, error };
}
```

### យកទៅប្រើប្រាស់ក្នុង Component៖
```jsx
import { useFetch } from './hooks/useFetch';

export default function UserProfile() {
  const { data: users, loading, error } = useFetch('https://jsonplaceholder.typicode.com/users');

  if (loading) return <p>កំពុងទាញយក...</p>;
  if (error) return <p className="text-red-500">កំហុស: {error}</p>;

  return (
    <ul>
      {users.map((u) => <li key={u.id}>{u.name}</li>)}
    </ul>
  );
}
```
