---
title: Managing Loading Skeletons, Data Rendering & Error States
description: សិក្សាពីយុទ្ធសាស្ត្រគ្រប់គ្រង 3 States នៃ Data Fetching (Loading Skeletons, Data Output, និង Error Handling)។
objectives:
  - យល់ពី Data Fetching State Machine (`loading`, `error`, `data`)
  - បង្កើត Loading Skeletons & Spinners សម្រាប់លើកកម្ពស់ User Experience (UX)
  - ចេះបង្កើត Graceful Error Recovery UI
module: module-4-effects-api
order: 4
quiz:
  - question: "ហេតុអ្វីបានជាការប្រើ Loading Skeletons ផ្តល់ UX ល្អជាង Spinner វិលធម្មតា?"
    options:
      - "ព្រោះវាជួយកាត់បន្ថយទំហំទិន្នន័យ API"
      - "ព្រោះវាបង្ហាញរចនាសម្ព័ន្ធ Layout ជាមុន (Perceived Performance) ជួយកាត់បន្ថយអារម្មណ៍នៃការរង់ចាំរបស់អ្នកប្រើប្រាស់"
      - "ព្រោះវាមិនបាច់ប្រើ CSS"
      - "ព្រោះវាទាញយកទិន្នន័យពី Server ដោយស្វ័យប្រវត្តិ"
    correctAnswer: 2
    explanation: "Skeleton Screens ជួយឱ្យ User មើលឃើញទម្រង់ Layout ដែលនឹងត្រូវបង្ហាញជាមុន ដែលជួយឱ្យ App មើលទៅមានអារម្មណ៍ថាលឿន និង Smooth ជាងមុន។"
---

## 📡 1. Data Fetching State Machine

```mermaid
flowchart TD
    subgraph StateMachine["Data Fetching State Machine (The 3 Essential States)"]
        direction TB
        Init["1. Mount Component: <code>loading = true, error = null</code>"] --> Req["2. HTTP Request: <code>fetch('https://api.example.com/users')</code>"]
        
        Req --> ResCheck{"3. Response Status OK (200)?"}
        
        ResCheck -->|Yes| Success["4. SUCCESS State 🟢<br><code>setUsers(data)</code><br><code>setLoading(false)</code><br>Render: &lt;UserTable /&gt;"]
        ResCheck -->|No / Network Failure| Failure["4. ERROR State 🔴<br><code>setError(err.message)</code><br><code>setLoading(false)</code><br>Render: &lt;Alert message={error} /&gt;"]
    end

    style StateMachine fill:#f8f9fa,stroke:#ced4da,color:#000
    style Init fill:#ebf8ff,stroke:#3182ce,color:#000
    style Success fill:#f0fff4,stroke:#38a169,color:#000
    style Failure fill:#fff5f5,stroke:#e53e3e,color:#000
```

---

## 🛠️ 2. កូដគំរូ: Data Fetching ជាមួយ Skeleton Loader

```jsx
import { useState, useEffect } from 'react';

export default function ArticleFeed() {
  const [articles, setArticles] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  const fetchArticles = async () => {
    setLoading(true);
    setError(null);
    try {
      const res = await fetch('https://jsonplaceholder.typicode.com/posts?_limit=5');
      if (!res.ok) throw new Error('មិនអាចទាញយកទិន្នន័យពី Server បានឡើយ!');
      const data = await res.json();
      setArticles(data);
    } catch (err) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    fetchArticles();
  }, []);

  // 1. ERROR STATE
  if (error) {
    return (
      <div className="p-6 bg-red-50 border border-red-200 rounded-2xl text-center">
        <p className="text-red-700 font-bold mb-3">⚠️ កំហុស៖ {error}</p>
        <button onClick={fetchArticles} className="px-4 py-2 bg-red-600 text-white rounded-lg font-bold">
          ព្យាយាមម្តងទៀត (Retry)
        </button>
      </div>
    );
  }

  // 2. LOADING SKELETON STATE
  if (loading) {
    return (
      <div className="space-y-4 max-w-lg mx-auto">
        {[1, 2, 3].map((i) => (
          <div key={i} className="p-5 border rounded-2xl animate-pulse space-y-3 bg-white">
            <div className="h-5 bg-gray-200 rounded w-3/4"></div>
            <div className="h-4 bg-gray-200 rounded w-full"></div>
            <div className="h-4 bg-gray-200 rounded w-1/2"></div>
          </div>
        ))}
      </div>
    );
  }

  // 3. SUCCESS DATA STATE
  return (
    <div className="space-y-4 max-w-lg mx-auto">
      {articles.map((article) => (
        <div key={article.id} className="p-5 bg-white border border-gray-100 rounded-2xl shadow-sm">
          <h3 className="font-bold text-gray-900 capitalize text-lg">{article.title}</h3>
          <p className="text-gray-600 text-sm mt-2">{article.body}</p>
        </div>
      ))}
    </div>
  );
}
```
