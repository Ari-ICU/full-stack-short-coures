---
title: JSON Web Tokens (JWT) Architecture & Signing
description: ស្វែងយល់ពីរចនាសម្ព័ន្ធ JWT (Header, Payload, Signature), ការបង្កើត Token, ការ Verify, និងការការពារសុវត្ថិភាព។
objectives:
  - យល់ពីរចនាសម្ព័ន្ធ 3 ផ្នែកនៃ JWT (Header.Payload.Signature)
  - ស្គាល់ភាពខុសគ្នារវាង Symmetric Signing (HS256) និង Asymmetric Signing (RS256/EdDSA)
  - ចេះប្រើ Library `jsonwebtoken` ដើម្បី sign និង verify tokens
  - យល់ពីច្បាប់សុវត្ថិភាព JWT (Never put secrets in Payload, Token Expiry)
module: module-5-authentication
order: 3
quiz:
  - question: "តើព័ត៌មាននៅក្នុង JWT Payload (ផ្នែកកណ្តាល) មានសុវត្ថិភាពកម្រិតណា?"
    options:
      - "វាត្រូវបាន Encrypt យ៉ាងសម្ងាត់ គ្មាននរណាអាចអានបានទេ"
      - "វាគ្រាន់តែត្រូវបាន Base64Url Encoded ប៉ុណ្ណោះ នរណាក៏អាច Decode អានបានដែរ ដូច្នេះដាច់ខាតកុំដាក់ Password ឬ Sensitive Data ក្នុង Payload"
      - "អាចអានបានតែជាមួយ Database Password ប៉ុណ្ណោះ"
      - "វារលុបបាត់ភ្លាមៗក្រោយ ១ វិនាទី"
    correctAnswer: 2
    explanation: "JWT គឺជា Signed Token មិនមែន Encrypted Token ឡើយ។ Payload គ្រាន់តែជា Base64-encoded string ដូច្នេះអ្នកណាក៏អាច decode មើលទិន្នន័យបានដែរ។ Signature គ្រាន់តែបញ្ជាក់ថាទិន្នន័យមិនត្រូវបានកែប្រែបន្លំប៉ុណ្ណោះ។"
---

## 🪙 អ្វីទៅជា JSON Web Token (JWT)?

**JWT (JSON Web Token)** គឺជាស្តង់ដារបើកចំហរ (RFC 7519) សម្រាប់ការបញ្ជូនព័ត៌មានរវាងភាគីពីរដោយសុវត្ថិភាព និងបង្រួមតូច (Compact & Self-contained)។

```mermaid
graph TD
    JWT["JSON Web Token: xxxxx.yyyyy.zzzzz"]
    
    H["1. Header (Algorithm & Token Type)<br/>{'alg': 'HS256', 'typ': 'JWT'}"]
    P["2. Payload (Claims & User Data)<br/>{'sub': 'user_123', 'role': 'admin', 'exp': 1714000000}"]
    S["3. Signature (HMACSHA256(H + '.' + P, SecretKey))"]
    
    JWT --> H
    JWT --> P
    JWT --> S
```

---

## ⚙️ ការប្រើប្រាស់ `jsonwebtoken` ក្នុង TypeScript

```bash
npm install jsonwebtoken
npm install -D @types/jsonwebtoken
```

### 1. Token Helper Service (`src/utils/jwt.ts`)
```typescript
import jwt, { SignOptions } from 'jsonwebtoken';

const JWT_SECRET = process.env.JWT_SECRET || 'super_secret_dev_key';
const JWT_EXPIRES_IN = '15m'; // Access Token អាយុខ្លី

export interface UserTokenPayload {
  userId: string;
  email: string;
  role: string;
}

export const signToken = (payload: UserTokenPayload, options?: SignOptions): string => {
  return jwt.sign(payload, JWT_SECRET, {
    expiresIn: JWT_EXPIRES_IN,
    algorithm: 'HS256',
    ...options,
  });
};

export const verifyToken = (token: string): UserTokenPayload => {
  try {
    return jwt.verify(token, JWT_SECRET) as UserTokenPayload;
  } catch (error: any) {
    if (error.name === 'TokenExpiredError') {
      throw new Error('Token ផុតកំណត់សុពលភាពហើយ (Token Expired)');
    }
    throw new Error('Token មិនត្រឹមត្រូវឡើយ (Invalid Token)');
  }
};
```

---

## 🛡️ ច្បាប់សុវត្ថិភាពសំខាន់ៗពេលប្រើ JWT

> [!WARNING]
> 1. **ហាមដាច់ខាត:** កុំដាក់ Passwords, Credit Card numbers, ឬ Private Secrets ទៅក្នុង JWT Payload។
> 2. **កំណត់ Expiration ជានិច្ច:** ត្រូវកំណត់អាយុកាល Token ខ្លី (ឧ. 15 នាទីទៅ 1 ម៉ោង) សម្រាប់ Access Token។
> 3. **ការពារ Secret Key:** ប្រើ Secret Key ដែលមានប្រវែងយ៉ាងតិច 256 bits (32+ bytes random characters)។
