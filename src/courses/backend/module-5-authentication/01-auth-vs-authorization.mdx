---
title: Authentication vs Authorization & Security Principles
description: យល់ច្បាស់ពីភាពខុសគ្នារវាងការផ្ទៀងផ្ទាត់អត្តសញ្ញាណ (Authentication) និងការអនុញ្ញាតសិទ្ធិ (Authorization), Session-based vs Token-based។
objectives:
  - យល់ច្បាស់ពីភាពខុសគ្នារវាង Authentication (401) និង Authorization (403)
  - ប្រៀបធៀប State-full Session-based Authentication vs Stateless Token-based Authentication
  - ស្គាល់ហានិភ័យសុវត្ថិភាពទូទៅ (CSRF, XSS, Session Hijacking)
  - យល់ពី Flow នៃការ Login និងការរក្សាទុក Security Context ក្នុង Backend
module: module-5-authentication
order: 1
quiz:
  - question: "ប្រសិនបើ User មិនទាន់ Login សោះ ហើយព្យាយាមបើកមើល Dashboard តើ Backend គួរឆ្លើយតប HTTP Status Code មួយណា?"
    options:
      - "400 Bad Request"
      - "401 Unauthorized (ខ្វះ ឬខុស Credentials សម្រាប់ Auth)"
      - "403 Forbidden (បាន Auth ស្គាល់ខ្លួនហើយ តែគ្មានសិទ្ធិ Role)"
      - "500 Internal Server Error"
    correctAnswer: 2
    explanation: "401 Unauthorized បញ្ជាក់ថា User មិនទាន់បាន Authenticate (មិនទាន់បញ្ជាក់ថាខ្លួនជាអ្នកណា)។ ចំណែក 403 Forbidden គឺ User ស្គាល់អត្តសញ្ញាណហើយ តែគ្មានសិទ្ធិចូលមើល Resource នោះ។"
---

## 🔐 ភាពខុសគ្នារវាង AuthN និង AuthZ

```mermaid
graph TD
    REQ["User Request"] --> A1["1. Authentication (AuthN)<br/>'តើអ្នកជានរណា?' (Who are you?)"]
    A1 -->|"Credentials Valid (Email + Password)"| A2["2. Authorization (AuthZ)<br/>'តើអ្នកមានសិទ្ធិធ្វើអ្វីខ្លះ?' (What can you do?)"]
    A1 -.->|"Invalid / Missing Token"| E401["401 Unauthorized"]
    A2 -->|"Admin Role Verified"| OK["Access Granted (200 OK)"]
    A2 -.->|"Customer Role Only"| E403["403 Forbidden"]
```

| លក្ខណៈ | Authentication (AuthN) | Authorization (AuthZ) |
|---|---|---|
| **និយមន័យ** | ការផ្ទៀងផ្ទាត់អត្តសញ្ញាណអ្នកប្រើប្រាស់ | ការត្រួតពិនិត្យសិទ្ធិ ឬតួនាទីក្នុងការប្រើប្រាស់ Resource |
| **សំណួរចម្បង** | "តើអ្នកជានរណា?" | "តើអ្នកអាចធ្វើអ្វីបានខ្លះ?" |
| **មធ្យោបាយ** | Passwords, OTP, Biometrics, OAuth (Google/GitHub) | Roles, Permissions, Policies, ACLs |
| **HTTP Status Code** | `401 Unauthorized` | `403 Forbidden` |

---

## ⚖️ Session-Based vs Token-Based Authentication

```mermaid
graph TD
    subgraph "Session-based Auth (Stateful)"
        C1["Client"] -->|"Login"| S1["Server"]
        S1 -->|"Store in Redis / Memory"| DB_S[("Session Store")]
        S1 -->|"Set-Cookie: session_id=xyz"| C1
        C1 -->|"Cookie sent automatically"| S1
    end
    
    subgraph "Token-based Auth / JWT (Stateless)"
        C2["Client"] -->|"Login"| S2["Server"]
        S2 -->|"Sign JWT Token (No DB store needed)"| C2
        C2 -->|"Authorization: Bearer <jwt>"| S2
    end
```

| លក្ខណៈសម្បត្តិ | Session-Based (Cookies) | Token-Based (JWT) |
|---|---|---|
| **Server State** | Stateful (ត្រូវការ RAM ឬ Redis ផ្ទុក Session) | Stateless (Server មិនបាច់ចាំ State ទេ) |
| **Scalability** | ពិបាក Scale ពេលមាន Servers ច្រើន (ត្រូវការ Sticky Sessions/Redis) | ងាយស្រួល Scale លើ Microservices & Serverless |
| **Mobile & Cross-domain** | ពិបាក Configure Cookie លើ Mobile Apps | ស្រួលប្រើជាមួយ REST API, Mobile Apps, SPAs |
| **Revocation** | ស្រួលលុបចោលភ្លាមៗ (Delete Session ពី Redis) | ត្រូវការ Refresh Token Rotation ឬ Blacklist |
