---
title: Cross-Origin Resource Sharing (CORS) Demystified
description: យល់ច្បាស់ពី Same-Origin Policy (SOP), Preflight OPTIONS requests, និងការ Configure CORS Middleware ត្រឹមត្រូវសម្រាប់ Production។
objectives:
  - យល់ពី Same-Origin Policy (SOP) របស់ Browser (Protocol + Domain + Port)
  - ស្គាល់យន្តការ Preflight Request (HTTP OPTIONS) សម្រាប់ Complex Requests
  - ចេះ Configure `cors` package សម្រាប់ Multiple Allowed Origins និង Credentials
  - ដោះស្រាយបញ្ហាទូទៅ 'Blocked by CORS policy' ដោយសុវត្ថិភាព
module: module-6-security
order: 2
quiz:
  - question: "ហេតុអ្វីបានជាការកំណត់ `cors({ origin: '*' })` ជាមួយ `credentials: true` ត្រូវបានបដិសេធដោយ Browser Security Standard?"
    options:
      - "ព្រោះ '*' មិនមែនជា URL ត្រឹមត្រូវ"
      - "ព្រោះស្តង់ដារសុវត្ថិភាព Browser ហាមឃាត់ការបញ្ជូន Cookies/Credentials ទៅកាន់ Wildcard Origin ('*') ដើម្បីការពារការលួចទិន្នន័យ (CSRF/Data Exfiltration)"
      - "ព្រោះ Express មិនគាំទ្រ CORS"
      - "ព្រោះ Node.js ត្រូវការ SSL"
    correctAnswer: 2
    explanation: "នៅពេលដែល Server អនុញ្ញាត `credentials: true` (Cookies, Authorization headers) នោះ `origin` ត្រូវតែបញ្ជាក់ Domain ជាក់លាក់ (Specific origin) មិនអាចប្រើ Wildcard `*` បានឡើយ។"
---

## 🌐 អ្វីទៅជា CORS & Same-Origin Policy?

**SOP (Same-Origin Policy)** គឺជាយន្តការសុវត្ថិភាពស្នូលរបស់ Web Browser ដែលហាមឃាត់ Script នៅលើ Domain មួយ (ឧ. `http://localhost:3000` - React) មិនឱ្យស្នើសុំទិន្នន័យពី Domain មួយផ្សេងទៀត (ឧ. `http://localhost:5000` - Express)។

**CORS (Cross-Origin Resource Sharing)** គឺជាវិធីដែល Backend Server ប្រាប់ Browser ថា Domain ណាខ្លះដែលត្រូវបានអនុញ្ញាត!

```mermaid
sequenceDiagram
    autonumber
    participant React as Frontend (localhost:3000)
    participant Browser as Browser Security Engine
    participant API as Express API (api.domain.com)

    React->>Browser: 1. Fetch POST /orders (with JSON & Auth Header)
    Note over Browser: Detects Complex Cross-Origin Request
    Browser->>API: 2. Preflight Request (OPTIONS /orders)
    API-->>Browser: 3. Headers: Access-Control-Allow-Origin: http://localhost:3000<br/>Access-Control-Allow-Methods: POST, GET, OPTIONS
    Note over Browser: Preflight Approved!
    Browser->>API: 4. Actual Request (POST /orders + Token)
    API-->>Browser: 5. 201 Created + JSON Data
    Browser-->>React: 6. Handover Data to JS Code
```

---

## 💻 ការ Configure CORS សម្រាប់ Production

```bash
npm install cors
npm install -D @types/cors
```

```typescript
// src/config/cors.ts
import cors, { CorsOptions } from "cors";

const allowedOrigins = [
  "http://localhost:3000",
  "https://my-ecommerce-app.com",
  "https://admin.my-ecommerce-app.com",
];

export const corsOptions: CorsOptions = {
  origin: (origin, callback) => {
    // អនុញ្ញាត Mobile Apps, Postman, ឬ Servers ដែលគ្មាន Origin Header
    if (!origin || allowedOrigins.includes(origin)) {
      callback(null, true);
    } else {
      callback(new Error(`CORS Blocked: Origin ${origin} មិនត្រូវបានអនុញ្ញាតឡើយ`));
    }
  },
  credentials: true, // អនុញ្ញាតការផ្ញើ Cookies & Auth Headers
  methods: ["GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"],
  allowedHeaders: ["Content-Type", "Authorization", "X-Requested-With"],
  maxAge: 86400, // Cache Preflight Response រយៈពេល 24 ម៉ោង (កាត់បន្ថយ OPTIONS calls)
};
```

### ការប្រើប្រាស់ក្នុង `app.ts`:
```typescript
import express from "express";
import cors from "cors";
import { corsOptions } from "./config/cors";

const app = express();
app.use(cors(corsOptions));
```
