---
title: Object-Relational Mapping (ORM) Concepts
description: អ្វីទៅជា ORM, គុណសម្បត្តិ និងគុណវិបត្តិ, និងការប្រៀបធៀបរវាង Raw SQL vs Query Builder vs ORMs (TypeORM / Prisma)。
objectives:
  - យល់ពីនិយមន័យ Object-Relational Mapping (ORM)
  - ស្គាល់ភាពខុសគ្នារវាង Data Mapper Pattern (TypeORM) និង Active Record Pattern
  - ប្រៀបធៀប Raw SQL, Knex (Query Builder), TypeORM, និង Prisma
  - យល់ពីគុណសម្បត្តិ (Type safety, Productivity) និង Trade-offs (Performance, N+1 Query Problem)
module: module-4-orm
order: 1
quiz:
  - question: "តើអ្វីជាអត្ថប្រយោជន៍ចម្បងនៃការប្រើប្រាស់ ORM ក្នុង TypeScript Project?"
    options:
      - "ORM ធ្វើឱ្យ Server មិនបាច់ភ្ជាប់ Internet"
      - "ផ្តល់នូវ Type Safety ពេញលេញ, Auto-completion ក្នុង IDE, និងការបំប្លែងទិន្នន័យពី Relational Tables ទៅជា TypeScript Objects ដោយស្វ័យប្រវត្តិ"
      - "ORM ដំណើរការលឿនជាង Raw C++ Code ជានិច្ច"
      - "ORM លុបបំបាត់តម្រូវការយល់ដឹងពី Database"
    correctAnswer: 2
    explanation: "ORM ផ្តល់នូវ Type-safe abstraction លើ Database Tables ដែលជួយកាត់បន្ថយ Bug ពីការវាយឈ្មោះ Column ខុស ព្រមទាំងបង្កើនល្បឿនអភិវឌ្ឍន៍ (Developer Productivity) យ៉ាងខ្លាំង។"
---

## 🎩 អ្វីទៅជា ORM?

**ORM (Object-Relational Mapping)** គឺជាបច្ចេកវិទ្យាដែលធ្វើស្ពានតភ្ជាប់រវាង **Object-Oriented / Typed Code (TypeScript/JavaScript Classes)** និង **Relational Database (Tables, Columns, Foreign Keys)**។

```mermaid
graph LR
    subgraph "Application Layer (TypeScript)"
        OBJ["User Entity Instance<br/>user.name = 'Dara'<br/>user.orders = [...]"]
    end
    
    subgraph "ORM Translation Engine (TypeORM)"
        MAPPER["Data Mapper / Metadata Engine<br/>• Generates Safe SQL<br/>• Maps Rows to Class Instances"]
    end
    
    subgraph "Database Layer (PostgreSQL)"
        DB[("users & orders tables<br/>(Rows & Foreign Keys)")]
    end
    
    OBJ <--> MAPPER
    MAPPER <--> DB
```

---

## 📊 ការប្រៀបធៀប Data Access Patterns

| វិធីសាស្ត្រ | ឧទាហរណ៍ Tools | គុណសម្បត្តិ | គុណវិបត្តិ |
|---|---|---|---|
| **Raw SQL** | `pg`, `mysql2` | ដំណើរការលឿនបំផុត (Zero Overhead), គ្រប់គ្រង SQL 100% | គ្មាន Type-Safety, ងាយនឹង typo, ពិបាក Re-factor |
| **Query Builder** | `Knex.js`, `Kysely` | បង្កើត Query dynamic តាម Javascript, ស្អាត | នៅតែត្រូវសរសេរ Schema និង Types ដាច់ដោយឡែក |
| **Full ORM** | `TypeORM`, `Prisma`, `Drizzle` | Type-Safe 100%, Auto-migration, Relationships ងាយស្រួល | មាន Performance Overhead បន្តិចបន្តួច, អាចជួប N+1 Problem បើមិនប្រយ័ត្ន |

---

## 🏛️ Active Record vs Data Mapper Patterns

TypeORM គាំទ្រ Pattern ធំៗទាំងពីរ៖

### 1. Active Record Pattern
Entity ផ្ទុកទាំងទិន្នន័យ និង Logic នៃ Database Operations ផ្ទាល់ខ្លួន៖
```typescript
// Active Record: ហៅ method លើ Class ផ្ទាល់
const user = await User.findOneBy({ email: "sok@example.com" });
user.isActive = false;
await user.save();
```

### 2. Data Mapper Pattern (Recommended for Large Architecture)
ញែកដាច់រវាង Data Model (Entity) និង Database Access (Repository)៖
```typescript
// Data Mapper: ប្រើ Repository ដាច់ដោយឡែក
const userRepository = AppDataSource.getRepository(User);
const user = await userRepository.findOneBy({ email: "sok@example.com" });
if (user) {
  user.isActive = false;
  await userRepository.save(user);
}
```

> [!TIP]
> ក្នុងវគ្គសិក្សានេះ យើងនឹងប្រើប្រាស់ **Data Mapper Pattern** ព្រោះវាជាស្ថាបត្យកម្មកម្រិត Enterprise ដែលជួយឱ្យ Codebase ងាយស្រួលធ្វើ Unit Test និង Maintain!
